<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://chao-x.github.io</id>
    <title>知识在于积累</title>
    <updated>2020-10-21T10:33:17.613Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://chao-x.github.io"/>
    <link rel="self" href="https://chao-x.github.io/atom.xml"/>
    <logo>https://chao-x.github.io/images/avatar.png</logo>
    <icon>https://chao-x.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, 知识在于积累</rights>
    <entry>
        <title type="html"><![CDATA[HTTP协议]]></title>
        <id>https://chao-x.github.io/post/http-xie-yi/</id>
        <link href="https://chao-x.github.io/post/http-xie-yi/">
        </link>
        <updated>2020-10-11T11:52:13.000Z</updated>
        <content type="html"><![CDATA[<h1 id="http协议">HTTP协议</h1>
<h2 id="1概述">1.概述</h2>
<ul>
<li>HTTP，超文本传输协议，是一种应用层协议</li>
<li>Web -  www 三个要素
<ul>
<li>html：页面展示所使用的语言</li>
<li>URL 页面位置</li>
<li>HTTP/HTTPS 使用的传输协议</li>
</ul>
</li>
</ul>
<blockquote>
<p>URL统一资源定位符，URI统一资源标识符</p>
<p>URL是URI的子集，URL是URI的一种方法</p>
<p>举例：一个人的URI就是能唯一可以标识该人的身份，可以是身份证号，可以是他的住址加上他的姓名</p>
</blockquote>
<ul>
<li>HTTP是基于IP协议和TCP协议的</li>
<li>在使用域名的情况下还要先使用DNS协议（应用层协议）</li>
</ul>
<h2 id="2报文">2.报文</h2>
<ul>
<li>
<p>报文的纯文本的，会被转换成以ASCII码格式传输</p>
</li>
<li>
<p>请求报文由四部分组成</p>
<ul>
<li>请求行</li>
<li>请求头</li>
<li>换行（CRLF）</li>
<li>消息体</li>
</ul>
</li>
<li>
<p>响应报文也由四部分组成</p>
<ul>
<li>状态行</li>
<li>响应头</li>
<li>换行（CRLF）</li>
<li>消息体</li>
</ul>
</li>
</ul>
<h4 id="请求报文">请求报文</h4>
<ul>
<li>请求行
<ul>
<li>请求方法
<ul>
<li>GET / POST / PUT /DELETE / HEAD</li>
</ul>
</li>
<li>URL
<ul>
<li>结合请求头Host属性可以使用相对路径</li>
</ul>
</li>
<li>HTTP版本</li>
</ul>
</li>
<li>请求头
<ul>
<li>Accept：告诉服务端，客户端浏览器可以处理（接收）什么类型的响应
<ul>
<li>text/html</li>
<li>img/png</li>
<li>img/*（一种类别所有类型）</li>
<li>*/ *（所有类别所有类型）</li>
</ul>
</li>
<li>Accept-Encoding：能接收的编码</li>
<li>Connection：连接状态</li>
<li>User-Agent 作用：指明客户端使用的操作系统、浏览器版本和名称</li>
<li>Content-Type：消息体的类型</li>
</ul>
</li>
</ul>
<h2 id="3http各版本">3.HTTP各版本</h2>
<p>一共有四个版本</p>
<ul>
<li>HTTP 0.9</li>
<li>HTTP 1.0</li>
<li>HTTP1.1</li>
<li>HTTP2</li>
</ul>
<blockquote>
<p>HTTP3即将到来，放弃了TCP协议，而是用UDP协议的某种实现，这里不再多述</p>
</blockquote>
<h4 id="http-09">HTTP 0.9</h4>
<ul>
<li>只支持GET方法</li>
<li>没有请求头概念</li>
<li>服务端响应后立即关闭TCP连接</li>
<li>传输内容只支持html文本传输</li>
</ul>
<blockquote>
<p>​    GET /index.html</p>
</blockquote>
<h4 id="http-10">HTTP 1.0</h4>
<ul>
<li>增加了POST /  PUT / DELETE / HEAD 方法，状态码等</li>
<li>可以指定HTTP版本号</li>
<li>增加了请求头和响应头的概念</li>
<li>传输内容增加了图片，音频等</li>
<li>服务端响应后也是立即关闭TCP连接，如果要长连接则需要设置Connection属性</li>
</ul>
<h4 id="http-11">HTTP 1.1</h4>
<ul>
<li>长连接，新增请求头的Connection属性，属性值为keep-alive表示为长连接（HTTP 1.1默认），close表示断开（HTTP 1.0默认）</li>
</ul>
<blockquote>
<p>长连接可以通过设置Keep-Alive属性来设置超时时间以及最大请求数量。</p>
<p>这里的时间如果超过了TCP的KeepAlive时间（如果已经开启）会以TCP为准</p>
<p>大部分应用都会实现心跳包功能，客户端向服务端发送心跳</p>
</blockquote>
<ul>
<li>管道化：可以不等待前一个请求返回，发送下一个请求。但是响应顺序还是和请求顺序一致</li>
<li>缓存处理：新增字段cache-control</li>
</ul>
<blockquote>
<p>当浏览器请求资源时，先看是否有缓存的资源，如果有缓存，直接取，不会再发请求，如果没有缓存，则发送请求。 通过设置字段cache-control来控制缓存。</p>
</blockquote>
<ul>
<li>断点传输</li>
</ul>
<blockquote>
<p>在上传/下载资源时，如果资源过大，将其分割为多个部分，分别上传/下载，如果遇到网络故障，可以从已经上传/下载好的地方继续请求，不用从头开始，提高效率</p>
</blockquote>
<h4 id="http-2">HTTP 2</h4>
<ul>
<li>二进制分帧</li>
</ul>
<blockquote>
<p>Http 2以前采用文本协议</p>
</blockquote>
<ul>
<li>多路复用</li>
</ul>
<blockquote>
<p>所有请求都是通过一个TCP连接完成，与管道不同，响应的顺序不必按照请求顺序返回</p>
</blockquote>
<ul>
<li>头部压缩</li>
</ul>
<blockquote>
<p>一般而言消息体都会经过压缩（gzip或者本身就是二进制），而头部是不压缩的，是文本文件类型。而随着头部的内容越来越多，为了加快传输效率，HTTP 2把头部也进行了压缩</p>
</blockquote>
<h2 id="4get和posthead的区别">4.GET和POST，HEAD的区别</h2>
<p>POST</p>
<ul>
<li>最简单的区别：GET把请求参数url中而POST放在消息体重</li>
<li>GET请求结果会被浏览器主动缓存，而POST不会除非主动设置</li>
<li>然后GET在浏览器回退时会从之前的缓存拿到结果，POST会再次提交请求</li>
<li>GET请求的URL可以作为书签而POST不行</li>
<li>GET只支持URL编码而POST可支持多种编码方式</li>
</ul>
<p>HEAD</p>
<ul>
<li>不会返回消息体，只返回头部</li>
<li>一般用于
<ul>
<li>只请求资源首部</li>
<li>检测超链接的有效性</li>
<li>检查网页是否被修改</li>
</ul>
</li>
</ul>
<h2 id="5http状态码">5.HTTP状态码</h2>
<ul>
<li>1开头表示请求已经接受需要进一步处理（临时响应）
<ul>
<li>100：已经收到一部分请求，正在等待其他部分。请求者应该继续执行操作</li>
<li>101：需要更换协议，服务器已经确认并准备切换</li>
</ul>
</li>
<li>2开头表示成功接收了请求的状态码
<ul>
<li>200：服务器已经处理了请求</li>
<li>201：已创建</li>
<li>202：已接收（可能尚未处理）</li>
</ul>
</li>
<li>3开头表示要完成请求需要进一步操作（重定向）
<ul>
<li>301：永久移动，URL已经移动到新的位置，会自动将请求者转到新的位置</li>
<li>302：临时移动</li>
</ul>
</li>
<li>4开头表示客户端可能发生了错误，妨碍了服务端的处理
<ul>
<li>400：URL请求语义有误，不能被服务器理解</li>
<li>401：没有身份认证或者身份认证有误，无权访问</li>
<li>402：保留暂未使用，本意是留给数字支付</li>
<li>403：表示客户端完成了身份认证，但是并没有响应的权限访问该资源</li>
<li>404：路劲不对，服务端找不到对应的资源</li>
</ul>
</li>
<li>以5开头表示服务端可能内部出现了错误，无法处理请求
<ul>
<li>500：服务端内部错误</li>
<li>501：没有能力处理请求，可能是没有能力识别该请求方法</li>
<li>502：错误网关。可能没有启动tomcat（没有启动服务器）</li>
<li>503：目前服务器无法使用（可能是维护或者过载）可以返回延迟时间</li>
<li>504：网关超时，服务器作为网关未能从上游服务器获得消息（可能是配置错误）</li>
</ul>
</li>
</ul>
<h2 id="6https协议">6.HTTPS协议</h2>
<ul>
<li>在HTTP的基础上加SSL层加密</li>
<li>SSL版本
<ul>
<li>TLS协议是SSL3.1版本</li>
<li>SSL1.0到3.0版本都是不安全的</li>
</ul>
</li>
</ul>
<h4 id="加密过程">加密过程</h4>
<figure data-type="image" tabindex="1"><img src="C:%5CUsers%5CChaoX%5CPictures%5Ccode%5C16796915-df1c6921f2c14805.png" alt="https.png" loading="lazy"></figure>
<ul>
<li>加密过程使用了对称加密以及非对称加密两种加密方式
<ul>
<li>加密内容使用对称加密</li>
<li>证书验证阶段用的是非对称加密</li>
</ul>
</li>
</ul>
<blockquote>
<p>对称加密：加密解密用的同一个钥匙，钥匙的传输的安全性存在隐患</p>
<p>非对称加密：公钥加密的信息只要私钥才能解开，反之亦然。（RSA算法）</p>
<p>服务端有私钥，将公钥发给浏览器就是非对称加密，但是公钥可能被截取，服务端向浏览器发的消息并不安全，浏览器发给服务端的会安全些（其实也不安全因为公钥不一定是服务器的，可能被替换）。</p>
<p>同理使用（两组公钥和私钥）或者（公钥私钥以及公钥加密过的对称秘钥）都是不安全的，因为都无法确保公钥是否被替换</p>
<p>公钥被替换这种攻击叫做中间人攻击</p>
</blockquote>
<p>只要确保了公钥是属于服务器的就能保住安全型</p>
<ul>
<li>这里我们需要一个第三方机构（CA）来保证公钥的真实性</li>
<li>操作系统浏览器里面是有权威机构（CA）的根证书（CA公钥信息）的，逐层嵌套，相互保证可靠性。</li>
</ul>
<p>数字证书</p>
<ul>
<li>加密过程：首先服务端（网站）需要向CA机构申请证书，CA机构拥有这些权威的公钥以及自己的私钥。CA用私钥对网站的公钥 、域名等信息的哈希值进行加密（数字签名）然后和明文信息（网站的公钥 、域名等信息）共同组成了数字证书。</li>
<li>解密过程：首先有一个根证书，根证书里面的公钥可以解开下一个层证书的信息得到下一层的公钥，以此类推。然后得到了CA的公钥对证书的加密部分解密得到哈希值，对明文信息的使用哈希算法（哈希不可逆）得到哈希值与之对比，可以发现证书是否是服务器的，从而可以使用明文里的公钥了。</li>
</ul>
<blockquote>
<p>浏览器的私钥并没有参与数字签名的加密和解密过程</p>
</blockquote>
<h2 id="7输入一个url发生了什么">7.输入一个URL发生了什么</h2>
<ul>
<li>DNS解析</li>
<li>客户端和服务端的TCP连接</li>
<li>客户端发送HTTP请求</li>
<li>服务端响应请求返回报文</li>
<li>浏览器解析渲染页面</li>
<li>连接结束</li>
</ul>
<figure data-type="image" tabindex="2"><img src="https://user-gold-cdn.xitu.io/2018/9/23/1660543eb1adb41a?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt="img" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[《MySQL必知必会》学习笔记一]]></title>
        <id>https://chao-x.github.io/post/sql/</id>
        <link href="https://chao-x.github.io/post/sql/">
        </link>
        <updated>2020-10-09T00:52:49.000Z</updated>
        <content type="html"><![CDATA[<h1 id="mysql必知必会学习笔记一">《MySQL必知必会》学习笔记一</h1>
<h2 id="1sql概述">1.SQL概述</h2>
<ul>
<li>
<p>SQL(&quot;Structured Query Language&quot;)，结构化查询语言</p>
</li>
<li>
<p>SQL可以说是一种脚本语言，因为它是由解释器来执行的</p>
</li>
<li>
<p>主键，唯一标识一行，可以由一列或多列组成</p>
<ul>
<li>主键不能为空</li>
<li>最好不更新主键的值</li>
<li>最好不重用主键的值（如果删除了主表的某个主键，但没有删除从表对应的行，如果重用，可能会有错误的关联）</li>
<li>主键不要使用容易更改的值</li>
</ul>
</li>
</ul>
<h2 id="2mysql概述">2.MySQL概述</h2>
<ul>
<li>
<p>DBS=DSB+DBMS，数据库系统=数据库+数据库管理系统</p>
</li>
<li>
<p>MySQL是一种DBMS，是基于C/S架构即分为客户机和服务端模式。</p>
</li>
<li>
<p>MySQL不区分大小写</p>
</li>
<li>
<p>所有关于数据、数据增删改查都由服务端来完成</p>
</li>
<li>
<p>DBMS为服务端软件，客户端可以是MySQL提供的工具，包括脚本语言，程序设计语言</p>
</li>
<li>
<p>登录</p>
<ul>
<li>
<pre><code class="language-bash">mysql -u root -p
</code></pre>
</li>
</ul>
</li>
<li>
<p>默认端口：3306</p>
</li>
</ul>
<h2 id="3mysql命令">3.MySQL命令</h2>
<p><strong>注意</strong></p>
<ul>
<li>每个命令（最好）需要以分号来表示结束</li>
<li>多条语句必须以分号来表示结束</li>
</ul>
<p><strong>展示所有数据库</strong></p>
<pre><code class="language-bash">SHOW DATABASES
</code></pre>
<p><strong>选择数据库</strong></p>
<pre><code class="language-bash">USE database-name
</code></pre>
<p><strong>展示某个数据库的所有表</strong></p>
<ul>
<li>需要选择数据库</li>
</ul>
<pre><code class="language-bash">SHOW TABLES
</code></pre>
<p><strong>展示某个表的所有列及其属性</strong></p>
<pre><code class="language-bash">SHOW COLUMNS FROM table-name
</code></pre>
<ul>
<li>快捷方式</li>
</ul>
<pre><code class="language-bash">DESCRIBE table-name
</code></pre>
<p><strong>展示错误或警告信息</strong></p>
<pre><code class="language-bash">SHOW ERRORS
</code></pre>
<pre><code class="language-bash">SHOW WARNINGS
</code></pre>
<h2 id="4select初级命令">4.SELECT初级命令</h2>
<h3 id="一select基础">一.SELECT基础</h3>
<p><strong>简单的SELECT命令</strong></p>
<pre><code class="language-sql">SELECT order_num,cust_id
FROM orders;
</code></pre>
<p><strong>检测出所有行</strong></p>
<pre><code class="language-sql">SELECT *
FROM orders;
</code></pre>
<p><strong>检测出不同的行</strong></p>
<ul>
<li>不能指定部分列，使用DISTINCT关键字后，SELECT的所有列都会被要求不同（组合方式），单列可以有相同的内容</li>
</ul>
<pre><code class="language-sql">SELECT DISTINCT prod_id,vend_id
FROM products;
</code></pre>
<p><strong>返回指定的数量</strong></p>
<ul>
<li>不能多于指定的行数</li>
</ul>
<pre><code class="language-sql">SELECT prod_id
FROM products
LIMIT 5;
</code></pre>
<ul>
<li>从指定的行数开始返回指定的数量</li>
<li>注意第一行的行号为0</li>
<li>下面这个语句表示从第六行开始返回五行</li>
</ul>
<pre><code class="language-sql">SELECT prod_id
FROM products
LIMIT 5,5;
</code></pre>
<p><strong>使用完全限定名称</strong></p>
<ul>
<li>完全限定列名</li>
<li>完全限定表名</li>
</ul>
<pre><code class="language-sql">SELECT products.prod_id
FROM formysql.products;
</code></pre>
<h3 id="二order-by排序">二.ORDER BY排序</h3>
<ul>
<li>
<p>关键字：ORDER BY</p>
</li>
<li>
<p>必须在WHERE和FROM后面</p>
</li>
<li>
<p>默认不分区大小写，A和a是一样</p>
</li>
<li>
<p>命令包含字句组成，SELECT中的FROM就是一个字句，同理ORDER BY也是</p>
</li>
</ul>
<p><strong>升序排序</strong></p>
<pre><code class="language-sql">SELECT prod_id,prod_name
FROM products
ORDER BY prod_id;
</code></pre>
<p><strong>多个列排序</strong></p>
<ul>
<li>按顺序，先按第一个再按后一个排序</li>
</ul>
<pre><code class="language-sql">SELECT prod_id,prod_name
FROM products
ORDER BY prod_id,pro_name
</code></pre>
<p><strong>指定排序顺序</strong></p>
<ul>
<li>默认为升序排序，关键字为ASC</li>
<li>可以使用降序排序，关键字为DESC</li>
<li>下面语句先根据id降序排序，再根据name升序排序</li>
<li>想要在多个列降序排序，必须为每个列使用DESC关键字</li>
</ul>
<pre><code class="language-sql">SELECT prod_id,prod_name
FROM products
ORDER BY prod_id DESC,prod_name;
</code></pre>
<p><strong>和LIMIT配合使用</strong></p>
<ul>
<li>LIMIT必须在ORDER BY后面使用</li>
<li>注意当使用来分页时，如果相同的列会随机返回，需要注意使用</li>
</ul>
<pre><code class="language-sql">SELECT prod_id,prod_name
FROM products
ORDER BY prod_id DESC,prod_name
LIMIT 5,5;
</code></pre>
<h3 id="三where过滤数据">三.WHERE过滤数据</h3>
<ul>
<li>WHERE关键字</li>
<li>操作符：=，&lt;&gt;或者=!，&lt;，&gt;，&lt;=，&gt;=，BETWEEN，IS NULL</li>
<li>字符串必须使用引号</li>
<li>如果使用ORDER BY关键字，必须在WHERE后面</li>
</ul>
<pre><code class="language-sql">SELECT prod_id,prod_name,prod_price
FROM products
WHERE prod_price = 2.5;
</code></pre>
<p><strong>范围筛选BETWEEN</strong></p>
<pre><code class="language-sql">SELECT prod_id,prod_name,prod_price
FROM products
WHERE prod_price BETWEEN 2 AND 3;
</code></pre>
<p><strong>空值检查 IS NULL</strong></p>
<pre><code class="language-sql">SELECT prod_id,prod_name,prod_price
FROM products
WHERE prod_price IS NULL;
</code></pre>
<p><strong>AND组合WHERE字句</strong></p>
<ul>
<li>可以使用括号防止混淆</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_id,prod_name,prod_price
FROM products
WHERE (prod_price BETWEEN 2 AND 5) AND vend_id=1003;
</code></pre>
<p><strong>OR组合WHERE语句</strong></p>
<ul>
<li>AND的计算优先级是比OR高的</li>
<li>可以使用括号改变优先级</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_id,prod_name,prod_price
FROM products
WHERE vend_id=1002 OR vend_id=1003 AND prod_price&gt;10;
</code></pre>
<p>等价于</p>
<pre><code class="language-sql">SELECT vend_id,prod_id,prod_name,prod_price
FROM products
WHERE vend_id=1002 OR (vend_id=1003 AND prod_price&gt;10);
</code></pre>
<p><strong>IN 指定具体范围</strong></p>
<ul>
<li>需要配合括号一起使用</li>
<li>IN操作符比OR操作更快</li>
<li>IN可以配合SELECT子语句一起使用</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_id,prod_name,prod_price
FROM products
WHERE vend_id IN (1002,1003)
ORDER BY vend_id;
</code></pre>
<p><strong>NOT 否定</strong></p>
<ul>
<li>MySQL支持NOT对IN，BETWEEN，EXISTS取反</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_id,prod_name,prod_price
FROM products
WHERE vend_id NOT IN (1002,1003)
ORDER BY vend_id;
</code></pre>
<h3 id="四通配符">四.通配符</h3>
<p><strong>LIKE 模糊匹配</strong></p>
<ul>
<li>必须和WHERE一起使用</li>
<li>通配符必须和LIKE一起使用，使用LIKE关键字配合通配符可以进行模糊匹配</li>
<li>通配符必须在引号中使用</li>
<li>搜索模式，由字符串和统配符组成，”j%e“即为在搜索模式中</li>
</ul>
<p><strong>% 任意字符出现任意次数</strong></p>
<ul>
<li>类似于正则表达式的*</li>
<li>可以匹配0个字符</li>
<li>通配符必须在引号中使用，不管是什么类型</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE vend_id LIKE '%0%';
</code></pre>
<p><strong>_ 匹配单个字符</strong></p>
<ul>
<li>必须且只能匹配一个字符</li>
<li>如果匹配1003则第二个SQL语句无法匹配到</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE vend_id LIKE '10_3';
</code></pre>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE vend_id LIKE '100_3';
</code></pre>
<p><strong>通配符的代价</strong></p>
<ul>
<li>使用时间相比较其他匹配较长</li>
<li>尽量不要把通配符放在最开始处，如&quot;%003&quot;，否则会花费大量时间</li>
</ul>
<h3 id="五正则表达式">五.正则表达式</h3>
<p><strong>REGEXP 关键字</strong></p>
<ul>
<li>
<p>REGEXP意思是正则表达式</p>
</li>
<li>
<p>和LIKE关键字一样，正则表达式必须和REGEXP一起使用</p>
</li>
<li>
<p>不加任何符号，表示包含即可</p>
</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP 'brid';
</code></pre>
<ul>
<li>和LIKE的区别，LIKE需要和整个串匹配，REGEXP包含即可</li>
</ul>
<p><strong>. 匹配任意字符</strong></p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP 'brid.';
</code></pre>
<p>错误示范：</p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP 'brid.';
</code></pre>
<ul>
<li>这样写其实是等价于 REGEXP 'brid'</li>
<li>该字符最好和位置字符搭配使用</li>
</ul>
<p><strong>OR  或匹配</strong></p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP 'brid|anvil|pack';
</code></pre>
<p>错误示范：</p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP 'brid|anvil|pack c';
</code></pre>
<ul>
<li>这样是不能匹配anvilc的，匹配的是pack c</li>
</ul>
<p>也可以使用</p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP '[ab]c';
</code></pre>
<p>或者等价于</p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP '[a|b]c';
</code></pre>
<p>错误示范：</p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP '[brid|anvil]c';
</code></pre>
<ul>
<li>上面是无效的，不能匹配bridc</li>
<li>[]只能匹配里面的一个字符,就是说上面等价于[bridanvil]c</li>
</ul>
<p><strong>范围匹配</strong></p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP '[1-9]';
</code></pre>
<p><strong>转义字符</strong></p>
<ul>
<li>
<p>当想要匹配 .  时，需要使用转义</p>
</li>
<li>
<p>使用 \\ 表示转义，如：</p>
</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP '\\.';
</code></pre>
<p><strong>特殊符号类</strong></p>
<figure data-type="image" tabindex="1"><img src="C:%5CUsers%5CChaoX%5CPictures%5Ccode%5CU%60YCCOUPGQD%5DJWJ$N_N%7B9H9.png" alt="img" loading="lazy"></figure>
<figure data-type="image" tabindex="2"><img src="C:%5CUsers%5CChaoX%5CPictures%5Ccode%5Cimage-20201009151003829.png" alt="image-20201009151003829" loading="lazy"></figure>
<p>示例一：</p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP '[[:digit:]]{4}';
</code></pre>
<ul>
<li>注意这里[:digit:]必须再加一层括号，因为不加括号实际为0123456789{4}</li>
</ul>
<p>示例二：</p>
<ul>
<li>
<p>之前匹配的都是要满足至少一个</p>
</li>
<li>
<p>如匹配[0-9] stick</p>
</li>
</ul>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP '[0-9] stickz?';
</code></pre>
<p><strong>定位符</strong></p>
<figure data-type="image" tabindex="3"><img src="C:%5CUsers%5CChaoX%5CPictures%5Ccode%5Cimage-20201009153346508.png" alt="image-20201009153346508" loading="lazy"></figure>
<p>示例：</p>
<pre><code class="language-sql">SELECT vend_id,prod_name
FROM products
WHERE prod_name REGEXP '^[0-9]';
</code></pre>
<ul>
<li>^可以在否定一个集合，<mark>这里存在疑问</mark></li>
</ul>
<p><strong>不用数据库使用正则表达式</strong></p>
<pre><code class="language-sql">SELECT 'HELLO'
REGEXP 'H'
</code></pre>
<h3 id="六函数的使用">六.函数的使用</h3>
<p><strong>Concat 拼接字段</strong></p>
<pre><code class="language-sql">SELECT Concat(vend_name,'(',vend_country,')')
FROM vendors
ORDER BY vend_name;
</code></pre>
<figure data-type="image" tabindex="4"><img src="C:%5CUsers%5CChaoX%5CPictures%5Ccode%5CX9W5%5DR5Z9DS%60%5BA%25KE7@Z4%7BO.png" alt="img" loading="lazy"></figure>
<p><strong>Trim 去掉空格</strong></p>
<ul>
<li>
<p>Trim 去掉作用两边的空格</p>
</li>
<li>
<p>LTrim 去掉左空格</p>
</li>
<li>
<p>RTrim 去掉右空格</p>
</li>
</ul>
<pre><code class="language-sql">SELECT Concat(Trim(vend_name),'(',vend_country,')')
FROM vendors
ORDER BY vend_name;
</code></pre>
<p><strong>AS 使用别名</strong></p>
<pre><code class="language-sql">SELECT Concat(Trim(vend_name),'(',vend_country,')') AS titile
FROM vendors
ORDER BY vend_name;
</code></pre>
<ul>
<li>如果客户机需要一个不能函数空格的列名，而该表的列名又有空格，可以使用别名</li>
</ul>
<p><strong>算术计算</strong></p>
<pre><code class="language-sql">SELECT quantity,item_price,quantity%item_price AS expanded_price
FROM orderitems;
</code></pre>
<ul>
<li>支持基本的算术计算，包括取余%</li>
<li>特殊用途：</li>
</ul>
<pre><code class="language-sql">SELECT 8*8;
</code></pre>
<pre><code class="language-sql">SELECT Now();
</code></pre>
<p><strong>转换大小写</strong></p>
<ul>
<li>Upper()转换大小</li>
<li>Lower()转换小写</li>
</ul>
<p><strong>常用函数</strong></p>
<figure data-type="image" tabindex="5"><img src="C:%5CUsers%5CChaoX%5CPictures%5Ccode%5Cimage-20201009160424141.png" alt="image-20201009160424141" loading="lazy"></figure>
<ul>
<li>关于其他函数，这里不再多述</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ARP协议]]></title>
        <id>https://chao-x.github.io/post/arp-xie-yi/</id>
        <link href="https://chao-x.github.io/post/arp-xie-yi/">
        </link>
        <updated>2020-09-30T12:47:56.000Z</updated>
        <content type="html"><![CDATA[<h1 id="arp协议">ARP协议</h1>
<h2 id="1概述">1.概述</h2>
<ul>
<li>
<p>地址解析协议，ARP协议在TCP/IP协议族中是属于网络层的，而在OSI七层模型中属于数据链路层。</p>
</li>
<li>
<p>它的主要作用是将IP地址和MAC地址相对应。</p>
</li>
<li>
<p>ARP协议是局域网内部协议。它解决的是同一局域网内部的主机或路由器的 <strong>IP 地址</strong>和其对应的<strong>硬件地址</strong>的映射问题。</p>
</li>
</ul>
<h2 id="2报文">2.报文</h2>
<figure data-type="image" tabindex="1"><img src="https://chao-x.github.io/post-images/1601303648769.png" alt="" loading="lazy"></figure>
<ul>
<li>
<p>ARP协议一共有9个字段</p>
</li>
<li>
<p>字段4为MAC地址类型，以太网类型为1</p>
</li>
<li>
<p>字段5为要映射的协议类型，IPv4协议为0x0800</p>
</li>
<li>
<p>字段8为操作类型字段，值为1为ARP请求，2为ARP应答，3为RARP请求，4为RARP应答</p>
</li>
</ul>
<h4 id="arp请求报文">ARP请求报文：</h4>
<figure data-type="image" tabindex="2"><img src="https://chao-x.github.io/post-images/1601303678474.png" alt="" loading="lazy"></figure>
<h4 id="arp应答报文">ARP应答报文：</h4>
<figure data-type="image" tabindex="3"><img src="https://chao-x.github.io/post-images/1601303703039.png" alt="" loading="lazy"></figure>
<h2 id="3arp高速缓存">3.ARP高速缓存</h2>
<ul>
<li>
<p>ARP缓存是一个缓冲区，本质就是&lt;IP地址，MAC地址&gt;的映射</p>
</li>
<li>
<p>当为已知的IP请求物理地址时，先在本地ARP缓存中查看，不存在才发送ARP请求向局域网查询。</p>
</li>
<li>
<p>ARP缓存表项的生存时间TTL，用于删除映射</p>
</li>
<li>
<p>ARP缓存包含动态和静态项目：</p>
<ul>
<li>
<p>动态项目随时间推移自动添加和删除，每个动态ARP缓存项都有都设置了TTL(生存时间)，Windows下TTL一般不超过10分钟。</p>
</li>
<li>
<p>静态 ARP 缓存条目是永久性的，可以使用 TCP/IP 工具手动添加和删除。静态 ARP 缓存条目用来对常用的本地IPv4地址**（例如路由器和服务器的 IPv4 地址）**的ARP请求。</p>
</li>
</ul>
</li>
<li>
<p>ARP高速缓存的优缺点</p>
<ul>
<li>
<p>优点：从ARP高速缓存的使用中可以看到，ARP高速缓存可以减小广播量，进而减小网络通信量，提高计算机之间的通信效率。</p>
</li>
<li>
<p>缺点：造成安全隐患，参考免费ARP的ARP攻击</p>
</li>
</ul>
</li>
</ul>
<h2 id="4免费arp">4.免费ARP</h2>
<ul>
<li>
<p>免费ARP指主机发送ARP查找自己的IP地址，通常发生在系统引导期间进行接口配置时。</p>
</li>
<li>
<p>与标准ARP的区别就是免费ARP分组的目的IP地址字段封装的是自己的IP地址，即向所在网络请求自己的MAC地址。</p>
</li>
</ul>
<h4 id="作用">作用</h4>
<ul>
<li>一个主机可以通过它来确定另一个主机是否设置了相同的 IP地址。</li>
</ul>
<p>正常情况下发送免费ARP请求不会收到ARP应答，如果收到了一个ARP应答，则说明网络中存在与本机相同的IP地址的主机，发生了地址冲突。</p>
<ul>
<li>更新其他主机高速缓存中旧的硬件地址进行。</li>
</ul>
<p>如果发送免费ARP的主机正好改变了硬件地址，如更换了接口卡。</p>
<p>其他主机接收到这个ARP请求的时候，发现自己的ARP高速缓存表中存在对应的IP地址，但是MAC地址不匹配，那么就需要利用接收的ARP请求来更新本地的ARP高速缓存表表项。</p>
<ul>
<li>ARP防御，网关利用免费ARP防止ARP攻击</li>
</ul>
<p>有些网关设备在一定的时间间隔内向网络主动发送免费ARP报文，让网络内的其他主机更新ARP表项中的网关MAC地址信息，以达到防止或缓解ARP攻击的效果。</p>
<ul>
<li>ARP攻击，利用免费ARP进行ARP攻击</li>
</ul>
<p>ARP协议并不只在发送了ARP请求才接收ARP应答，<strong>计算机只要接收到ARP应答数据包，就会使用应答中的IP和MAC地址对本地的ARP缓存进行更新。</strong></p>
<p>主机可以构造虚假的免费ARP应答，将ARP的源MAC地址设为错误的MAC地址，并把这个虚假的免费ARP应答发送到网络中，那么所有接收到这个免费ARP应答的主机都会更新本地ARP表项中相应IP地址对应的MAC地址。更新成功后，这些主机的数据报文就会被转发到错误的MAC地址，从而实现了<strong>ARP欺骗</strong>的攻击。</p>
<h2 id="5代理arp">5.代理ARP</h2>
<h4 id="定义">定义</h4>
<ul>
<li>代理ARP就是通过使用一个主机(通常为路由器)，来作为指定的设备使用自己的 MAC 地址来对另一设备的ARP请求作出应答。</li>
</ul>
<h4 id="为什么需要代理arp">为什么需要代理ARP？</h4>
<ul>
<li>
<p>先要了解，路由器的重要功能之一就是把局域网的广播包限制在该网内，阻止其扩散，否则会造成网络风暴。</p>
</li>
<li>
<p>ARP请求是个广播包，它询问的对象如果在同一个局域网内，就会收到应答。但是如果<strong>询问的对象不在同一个局域网该如何处理</strong>？路由器就提供的代理ARP为这个问题提供了解决方案。</p>
</li>
</ul>
<h4 id="工作过程">工作过程</h4>
<p>两台主机A和B处于同一网段但不同的广播段时，主机A发送ARP请求主机B的MAC地址时，因为路由器不转发广播包的原因，ARP请求只能到达路由器。如果路由器启用了代理ARP功能，并知道主机B属于它连接的网络，那么路由器就用自己接口的MAC地址代替主机B的MAC地址来对主机A进行ARP应答。主机A接收ARP应答，但并不知道代理ARP的存在。</p>
<h4 id="代理arp的优缺点">代理ARP的优缺点</h4>
<ul>
<li>
<p>优点：代理ARP能在不影响路由表的情况下添加一个新的Router，使子网对该主机变得透明化。一般代理ARP应该使用在主机没有配置默认网关或没有任何路由策略的网络上。</p>
</li>
<li>
<p>缺点：从工作工程可以看到，这其实是一种ARP欺骗。而且，通过两个物理网络之间的路由器的代理ARP功能其实互相隐藏了物理网络，这导致无法对网络拓扑进行网络概括。此外，代理ARP增加了使用它的那段网络的ARP流量，主机需要更大的ARP缓存空间，也不会为不使用ARP进行地址解析的网络工作。</p>
</li>
</ul>
<h2 id="6rarp">6.RARP</h2>
<ul>
<li>用于已知硬件地址，而IP地址未知的情况。</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[以太网协议]]></title>
        <id>https://chao-x.github.io/post/yi-tai-wang-xie-yi/</id>
        <link href="https://chao-x.github.io/post/yi-tai-wang-xie-yi/">
        </link>
        <updated>2020-09-30T12:46:38.000Z</updated>
        <content type="html"><![CDATA[<h2 id="以太网协议">以太网协议</h2>
<p>​	我们的主机要和外界互联网进行通信需要加入网络，我可以通过通过光纤，双绞线，无线电报等方式进行网络的连接，它们规定了网络的一些电气特征，主要的作用是传输0和1的电信号。这是物理层的作用。</p>
<p>​	单纯的0和1是没有意义的。在数据链路层中以太网协议将规定了解读电信号的方式。它把0和1进行分组，每一组为一帧。</p>
<p>以太网数据帧由三个部分组成：</p>
<p>头部，数据部，尾部</p>
<figure data-type="image" tabindex="1"><img src="https://img-blog.csdn.net/2018060615493515?watermark/2/text/aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2xpdWNoZW54aWE4/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70" alt="这里写图片描述" loading="lazy"></figure>
<p>​	目的地址和原地址都是MAC地址</p>
<p>​	MAC地址：网卡的地址，48位  6字节   12个16进制数表示 FF-FF-FF-FF-FF-FF*</p>
<p>​	<strong>目的地址</strong>有<strong>三类</strong>：为单播地址，广播地址，多播地址</p>
<blockquote>
<p>以太网数据帧的传输顺序和从左往右传输的，但是目的地址的低位先被传输。如 F1-E2的传输顺序为1F2E</p>
<p>单播地址：一对一传输，目的地址即为目的主机的MAC地址（第一个字节的最低位必须为0）</p>
<p>广播地址：一对多传输，目的地址为FF-FF-FF-FF-FF-FF-FF-FF</p>
<p>多播地址:一对多传输，目的地址最先发送的位必须为1（第一个字节的最低位），这样目的地址不会与任何MAC地址相同，多播数据可以被很多网卡接收</p>
</blockquote>
<p>​	<strong>类型</strong>：表示的是数据属于哪个上层协议（例如0x800，代表IP数据包；0x806，代表ARP数据包）。</p>
<p>在使用网卡进行数据包的发送与接收时，网卡已为我们完成了物理层的所有工作，驱动程序要做的是，在发送数据时，将目的地址、源地址、类型/长度、数据和填充这些值写入网卡，网卡自动计算其CRC并添加在数据帧尾部，同时对数据帧进行物理层的封装，最后将数据帧发送出去</p>
<p><strong>以太网基于CSMA/CD，以CSMA/CD作为MAC<a href="http://lib.csdn.net/base/datastructure">算法</a>的一类LAN称为以太网。CSMA/CD冲突避免的方法：先听后发、边听边发、冲突停止、随机延迟后重发。</strong></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[TCP协议]]></title>
        <id>https://chao-x.github.io/post/tcp-xie-yi/</id>
        <link href="https://chao-x.github.io/post/tcp-xie-yi/">
        </link>
        <updated>2020-09-30T02:50:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-tcp连接的特点"><strong>一. TCP连接的特点</strong></h2>
<p>​	提供面向连接的，可靠的传输层协议。每个TCP连接由TCP四元组唯一确认。</p>
<blockquote>
<p>TCP四元组：源端口号+ip首部中的源ip地址+目的端口号+ip首部中的目的ip地址，唯一的确定了一个tcp连接。对应编码级别的socket。</p>
</blockquote>
<h2 id="二报文"><strong>二.报文</strong></h2>
<h3 id="宏观位置"><strong>宏观位置</strong></h3>
<figure data-type="image" tabindex="1"><img src="https://chao-x.github.io/post-images/1601468959493.jpg" alt="" loading="lazy"></figure>
<h3 id="首部格式"><strong>首部格式</strong></h3>
<figure data-type="image" tabindex="2"><img src="https://chao-x.github.io/post-images/1601469000224.jpg" alt="" loading="lazy"></figure>
<blockquote>
<p>序号用于解决网络包乱序的问题，确认号用于解决丢包问题</p>
</blockquote>
<blockquote>
<p>窗口大小。接收端期望接收的字节数，用于滑动窗口。<strong>解决流量控制的问题</strong></p>
</blockquote>
<blockquote>
<p>校验和。TCP的校验和包括头部和数据部分，而IP协议只包括头部。<strong>解决数据正确性问题</strong></p>
</blockquote>
<blockquote>
<p>紧急指针，URG标识位置一时有效。紧急指针一般指向紧急报文序列的最后一位或者下一位。此时，收到紧急报文的TCP进入紧急状态，应该优先处理紧急报文。</p>
</blockquote>
<p><strong>标识位说明</strong></p>
<ul>
<li>URG：为1时，表示紧急指针有效</li>
<li>ACK：确认标识，确认收到，置为1时确认号有效</li>
<li>PSH：接收方应尽快把这个报文交给应用层</li>
<li>RST：复位标识，重建连接</li>
<li>SYN：建立新连接时，该位为0</li>
<li>FIN：关闭连接标识</li>
</ul>
<p><strong>选项：MSS 最长报文大小</strong></p>
<ul>
<li>最常见的可选字段</li>
<li>MSS只能出现在SYN时传过来（第一次握手和第二次握手时）</li>
<li>指明本端能接收的最大长度的报文段</li>
<li>建立连接时，双方都要发送MSS</li>
<li>如果不发送，默认为536字节</li>
</ul>
<h2 id="三面向连接的"><strong>三.面向连接的</strong></h2>
<p><strong>1.连接建立的“三次握手”</strong></p>
<p><strong>1.1 三次握手流程</strong></p>
<figure data-type="image" tabindex="3"><img src="https://chao-x.github.io/post-images/1601469021862.png" alt="" loading="lazy"></figure>
<p><strong>1.2 为什么是三次握手</strong></p>
<ul>
<li>tcp连接是全双工的，数据在两个方向上能同时传递。</li>
<li><strong>确保双方，同时能发数据和收数据</strong></li>
<li>第一次握手：确保了发送方能发数据</li>
<li>第二次握手：确保了接收方能收数据，接收方能发数据</li>
<li>第三次握手：确保了发送方能收数据</li>
</ul>
<p><strong>2. 连接关闭的“四次挥手”</strong></p>
<p><strong>2.1 为什么是四次挥手</strong></p>
<ul>
<li>因为tcp连接是全双工的，数据在两个方向上能同时传递。</li>
<li>同时tcp支持半关闭（发送一方结束发送还能接收数据的功能）。</li>
<li>因此每个方向都要单独关闭，且收到关系通知需要发送确认回复</li>
</ul>
<p><strong>2.2 为什么要支持半关闭</strong></p>
<ul>
<li>客户端需要通知服务端，它的数据已经传输完毕</li>
<li>同时仍要接收来自服务端的数据</li>
<li>使用半关闭的单连接效率要比使用两个tcp连接更好</li>
</ul>
<p><strong>2.3 四次握手流程</strong></p>
<figure data-type="image" tabindex="4"><img src="https://chao-x.github.io/post-images/1601469031434.png" alt="" loading="lazy"></figure>
<p><strong>3. 连接和关闭对应的状态</strong></p>
<p><strong>3.1</strong></p>
<p><strong>3.2 time_wait状态</strong></p>
<ul>
<li>也称为2MSL等待状态，MSL，报文段最大生存时间，根据不同的tcp实现自行设定。常用值为30s，1min，2min。linux一般为30s。</li>
<li>主动关闭的一方发送最后一个ack所处的状态</li>
<li>这个状态必须维持2MSL等待时间</li>
</ul>
<p><strong>3.2.1 为什么需要这么做？</strong></p>
<ul>
<li>设想一个场景，最后这个ack丢失了，接收方没有收到</li>
<li>这时候接收方会重新发送fin给发送方</li>
<li>这个等待时间就是为了防止这种情况发生，让发送方重新发送ack</li>
<li>总结：预留足够的时间给接收端收ack。</li>
</ul>
<p><strong>3.2.2 这么做的后果？</strong></p>
<ul>
<li>在这2MSL等待时间内，该连接（socket，ip+port）将不能被使用</li>
<li>很多时候linux上报too many open files，说端口不够用了，就需要检查一些代码里面是不是创建大量的socket连接，而这些socket连接并不是关闭后就立马释放的</li>
<li>客户端连接服务器的时候，一般不指定客户端的端口。因为客户端关闭然后立马启动，按照理论来说是会提示端口被占用。同样的道理，主动关闭服务器，2MSL时间内立马启动是会报端口被占用的错误</li>
<li>多并发的短连接情况下，会出现大量的Time_wait状态。这两个参数可以解决问题，但是它违背了tcp协议，是有风险的。参数为：tcp_tw_reuse和tcp_tw_recycle</li>
<li>如果是服务端开发，可设置keep-alive，让客户端主动关闭连接解决这个问题</li>
</ul>
<p><strong>4. 复位报文段</strong></p>
<p>一个报文段从源地址发往目的地址，只要出现错误，都会发出复位的报文段，首部字段的RST是用于“复位”的。这些错误包括以下情况</p>
<ul>
<li>端口没有在监听</li>
<li>异常中止：通过发送RST而不是fin来中止连接</li>
</ul>
<p><strong>5. 同时打开</strong></p>
<figure data-type="image" tabindex="5"><img src="https://chao-x.github.io/post-images/1601469115230.jpg" alt="" loading="lazy"></figure>
<ul>
<li>两个应用程序同时执行主动打开，称为“同时打开“</li>
<li>这种情况极少发生</li>
<li>两端同时发送SYN，同时进入SYN_SENT状态</li>
<li>打开一条连接而不是两条</li>
<li>要进行四次报文交换过程，“四次握手”</li>
</ul>
<p><strong>6. 同时关闭</strong></p>
<figure data-type="image" tabindex="6"><img src="https://chao-x.github.io/post-images/1601469164839.JPG" alt="" loading="lazy"></figure>
<ul>
<li>双方同时执行主动关闭</li>
<li>进行四次报文交换</li>
<li>状态和正常关闭不一样</li>
</ul>
<p><strong>7. 服务器对于并发请求的处理</strong></p>
<ul>
<li>SYN Queue队列的backlog，积压值是开始了第一次SYN的连接请求</li>
<li>Accept Queue的最大连接数是已经完成了三次握手的</li>
</ul>
<h2 id="三可靠的"><strong>三.可靠的</strong></h2>
<p><strong>1.数据的分片传输</strong></p>
<ul>
<li>成块数据传输：量大，报文段常常是满的，一般用于数据量较大的协议（应用层）。使用滑动窗口机制实现。应用于FTP，电子邮件</li>
<li>交互数据传输：量小，一般用于数据量较小的协议（应用层），交互性较强的应用。报文段为微小分组，大量微小分组，在广域网传输会增加拥堵的出现，所以TCP采用经受时延的确认以及Negle算法两种方式来减小拥堵的发生。</li>
</ul>
<p><strong>2. 交互数据的传输技术</strong></p>
<p>**2.1 捎带ACK **</p>
<ul>
<li>滞销ACK，并不立马发送ack确认，而是稍后发送，如果这段时间有数据要发送给对方，则直接捎带ack一起发送。</li>
<li>延时时间：绝大多数为200ms。不能超过500ms。？</li>
</ul>
<p><strong>2.2 Nagle算法</strong></p>
<ul>
<li>解决什么问题：微小分组导致在广域网出现的拥堵问题</li>
<li>Nagle 算法指出，当 TCP 发送了一个小的 segment (小于 MSS )，它必须等到接收了对方的 ACK 之后，才能继续发送另一个小的 segment 。那么在等待的过程中(一个 RTT 时间)， TCP 就能尽量多地将要发送的数据收集在一起，从而减少要发送的 segment 的数量。</li>
</ul>
<p><strong>3. 成块数据的传输</strong></p>
<ul>
<li>主要使用滑动窗口协议</li>
</ul>
<h2 id="四-滑动窗口协议"><strong>四. 滑动窗口协议</strong></h2>
<p><strong>1.  概述</strong></p>
<ul>
<li>解决了什么问题：发送方和接收方速率不匹配时，保证可靠传输和包乱序的问题</li>
<li>机制：接收方根据目前缓冲区大小，通知发送方目前能接收的最大值。发送方根据接收方的处理能力来发送数据。通过这种协调机制，防止接收端处理不过来。</li>
<li>窗口大小：接收方发给发送端的这个值称为窗口大小</li>
</ul>
<p><strong>2. tcp缓冲区的数据结构</strong></p>
<ul>
<li>接收端：
<ul>
<li>LastByteRead: 缓冲区读取到的位置</li>
<li>NextByteExpected：收到的连续包的最后一个位置</li>
<li>LastByteRcvd：收到的包的最后一个位置</li>
<li>中间空白区：数据没有到达</li>
</ul>
</li>
<li>发送端：
<ul>
<li>LastByteAcked: 被接收端ack的位置，表示成功发送确认</li>
<li>LastByteSent：发出去了，还没有收到成功确认的Ack</li>
<li>LastByteWritten：上层应用正在写的地方</li>
</ul>
</li>
</ul>
<p><strong>3. 滑动窗口示意图</strong></p>
<p><strong>3.1 初始时示意图</strong></p>
<figure data-type="image" tabindex="7"><img src="https://chao-x.github.io/post-images/1601469221247.JPG" alt="" loading="lazy"></figure>
<ul>
<li>黑框表示滑动窗口</li>
<li>#1表示收到ack确认的数据</li>
<li>#2表示还没收到ack的数据</li>
<li>#3表示在窗口中还没有发出的（接收方还有空间）</li>
<li>#4窗口以外的数据（接收方没空间）</li>
</ul>
<p><strong>3.2 滑动过程示意图</strong></p>
<figure data-type="image" tabindex="8"><img src="https://chao-x.github.io/post-images/1601469247647.JPG" alt="" loading="lazy"></figure>
<p><strong>3.3滑动窗口会引发的问题</strong></p>
<p><strong>零窗口</strong></p>
<ul>
<li>如何发生： 接收端处理速度慢，发送端发送速度快。窗口大小慢慢被调为0。此时待接受法处理完缓冲区的数据后，会发送ACK报文，但如果ACK报文丢失，发送发无法得知接受方的有空闲缓存，就会造成死锁。</li>
<li>如何解决：零窗口探测定时器，当发送发得知窗口为0后，会隔一段时间发送探测报文，了解接收端窗口的大小。（坚持定时器）</li>
</ul>
<p><strong>糊涂窗口综合征</strong></p>
<ul>
<li>如何发生：接收方太忙，取不完数据，导致发送方越来越小。最后只让发送方传几字节的数据。</li>
<li>如何解决：避免对小的窗口大小做响应。
<ul>
<li>发送端：前面说到的Nagle算法。</li>
<li>接收端：窗口大小小于某个值，阻止发送数据。窗口变大后再发。</li>
</ul>
</li>
</ul>
<h2 id="五-确认和重传"><strong>五. 确认和重传</strong></h2>
<p><strong>1.概念</strong></p>
<ul>
<li>tcp提供可靠的运输层，使用的方法是确认机制。</li>
<li>但是数据和确认都有可能丢失</li>
<li>tcp通过在发送时设置定时器解决这种问题</li>
<li>定时器时间到了还没收到确认，就重传该数据</li>
</ul>
<p><strong>2. tcp管理的定时器类型</strong></p>
<ul>
<li>重传定时器：等待收到确认（重传）RT0</li>
<li>坚持定时器：使窗口大小信息保持不断流动（零窗口）</li>
<li>保活定时器：检测空闲连接崩溃或重启（KeepAlive）</li>
<li>2MSL定时器：检测time_wait状态（四次挥手）</li>
</ul>
<blockquote>
<ul>
<li><code>RTT(Round Trip Time)</code>：一个连接的往返时间，即数据发送时刻到接收到确认的时刻的差值；</li>
<li><code>RTO(Retransmission Time Out)</code>：重传超时时间，即从数据发送时刻算起，超过这个时间便执行重传。</li>
</ul>
</blockquote>
<p><strong>3. 超时重传机制</strong></p>
<p><strong>3.1 背景</strong></p>
<ul>
<li>接收端给发送端的Ack确认只会确认最后一个连续的包</li>
<li>比如发送1,2,3,4,5共五份数据，接收端收到1,2，于是回ack3，然后收到4（还没收到3），此时tcp不会跳过3直接确认4，否则发送端以为3也收到了。这时你能想到的方法是什么呢？tcp又是怎么处理的呢？</li>
</ul>
<p><strong>3.1 被动等待的超时重传策略</strong></p>
<ul>
<li>直观的方法是：接收方不做任何处理，等待发送方超时，然后重传。
<ul>
<li>缺点：发送端不知道该重发3，还是重发3,4,5</li>
</ul>
</li>
<li>总之，都在被动等待超时，超时可能很长。所以tcp不采用此方法</li>
</ul>
<p><strong>3.2 主动的快速重传机制</strong></p>
<p><strong>3.2.1 概述</strong></p>
<ul>
<li>名称为：Fast Retransmit</li>
<li>不以实际驱动，而以数据驱动重传</li>
</ul>
<p><strong>3.2.2 实现原理</strong></p>
<ul>
<li>
<p>如果包没有送达，就一直ack最后那个可能被丢的包</p>
</li>
<li>
<p>发送方连续收到3相同的ack，就重传。不用等待超时</p>
</li>
</ul>
<figure data-type="image" tabindex="9"><img src="https://chao-x.github.io/post-images/1601469281781.JPG" alt="" loading="lazy"></figure>
<ul>
<li>
<p>图中发生1,2,3,4,5数据</p>
</li>
<li>
<p>数据1到达，发生ack2</p>
</li>
<li>
<p>数据2因为某些原因没有送到</p>
</li>
<li>
<p>后续收到3的时候，接收端并不是ack4，也不是等待。而是主动ack2</p>
</li>
<li>
<p>收到4,5同理，一直主动ack2</p>
</li>
<li>
<p>客户端收到三次ack2，就重传2</p>
</li>
<li>
<p>2收到后，结合之前收到的3,4,5，直接ack6</p>
</li>
</ul>
<p><strong>3.2.3 快速重传的利弊</strong></p>
<ul>
<li>解决了被动等待timeout的问题</li>
<li>无法解决重传之前的一个，还是所有的问题。</li>
<li>上面的例子中是重传2，还是重传2,3,4,5。因为并不清楚ack2是谁传回来的</li>
</ul>
<p><strong>3.3 SACK方法</strong></p>
<p><strong>3.3.1 概述</strong></p>
<ul>
<li>为了解决快速重传的缺点，一种更好的SACK重传策略被提出</li>
<li>基于快速重传，同时在tcp头里加了一个SACK的东西</li>
<li>解决了什么问题：客户端应该发送哪些超时包的问题</li>
</ul>
<p><strong>3.3.2 实现原理</strong></p>
<ul>
<li>SACK记录一个数值范围，表示哪些数据收到了</li>
</ul>
<p><strong>3.3.3 Duplicate SACK(D-SACK)</strong></p>
<ul>
<li>使用SACK标识的范围，还可以知道告知发送方，有哪些数据被重复接收了</li>
<li>可以让发送方知道：是发出去的包丢了，还是回来的ack包丢了</li>
</ul>
<p><strong>4. 超时时间的确定</strong></p>
<p><strong>4.1 背景</strong></p>
<ul>
<li>路由器和网络流量均会变化</li>
<li>所以超时时间肯定不能设置为一个固定值</li>
<li>超时长：重发慢，效率低，性能差</li>
<li>超时短：并没有丢就重发，导致网络拥塞，导致更多超时和更多重发</li>
<li>tcp会追踪这些变化，并相应的动态改变超时时间（RTO）</li>
</ul>
<p><strong>4.2 如何动态改变</strong></p>
<ul>
<li>每次重传的时间间隔为上次的一倍，直到最大间隔为64s，称为“指数退避”</li>
<li>首次重传到最后放弃重传的时间间隔一般为9min</li>
<li>依赖以往的往返时间计算（RTT）动态的计算</li>
</ul>
<p><strong>4.3 往返时间（RTT）的计算方法</strong></p>
<ul>
<li>并不是简单的ack时间和发送时间的差值。因为有重传，网络阻塞等各种变化的因素。</li>
<li>而是通过采样多次数值，然后做估算</li>
</ul>
<h2 id="5-拥塞控制"><strong>5. 拥塞控制</strong></h2>
<p><strong>5.1 为什么重传会引发拥塞</strong></p>
<ul>
<li>当网络延迟突然增加时，tcp会重传数据</li>
<li>但是过多的重传会导致网络负担加重，从而导致更大的延时和丢包，进入恶性循环</li>
<li>相比于流量控制，流量控制是作用于（有利于）接受者，防止太多的TCP报文接受者无法处理引起的丢包。而拥塞控制是有利于网络的。</li>
</ul>
<blockquote>
<p>cwnd 拥塞窗口</p>
<p>ssthresh 慢开始阈值</p>
<p>rwnd接受方窗口</p>
<p>RTT 报文往返的时间</p>
<p>RTO 超时重传时间</p>
<p>TTL IP报文的生存时间，每经过一次路由器就减一</p>
<p>MSL 网络中报文的最大生存时间，超过这个时间则丢弃报文</p>
</blockquote>
<p><strong>5.2 解决拥塞-拥塞控制的算法</strong></p>
<ul>
<li>
<p>慢启动</p>
<p>维护一个拥塞窗口，初始化为1，每有一个ACK，cwnd加一。所以慢开始每经过一个报文的确认后就加一，所以是以倍数增长的。</p>
</li>
<li>
<p>拥塞避免</p>
<p>当报文超过了慢开始阈值，就进入拥塞避免算法。</p>
<p>拥塞避免算法是每次经过一次RTT拥塞窗口就加一，而不是每次确认报文后加一，避免了倍数增长</p>
<p>当出现了报文丢失，就是乘法减小和加法增大算法。乘法减小值得是每有一次报文丢失，慢开始的阈值就为原来的二分之一大小</p>
</li>
<li>
<p>快速重传</p>
<p>没收到一个失序的报文就发送一次ACK，发送方接受到三次ACK则重传。</p>
</li>
<li>
<p>快速恢复</p>
<p>传统的恢复是把拥塞窗口置为0，然后启用慢开始。这样的弊端是如果有一组报文由于某种异常丢失，并且这些报文来自很多发送端，但是网络中并没有拥塞，会导致网络的传输能力被浪费。</p>
<p>快恢复是把cwnd和慢开始的阈值都为原来的0.5，执行拥塞避免即加法增大。</p>
</li>
</ul>
<p>综合考虑流量控制和拥塞控制：</p>
<p>​	发送方的滑动窗口应该为拥塞窗口和接收方可接受报文的最小值</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[操作系统]]></title>
        <id>https://chao-x.github.io/post/cao-zuo-xi-tong/</id>
        <link href="https://chao-x.github.io/post/cao-zuo-xi-tong/">
        </link>
        <updated>2020-09-19T11:07:09.000Z</updated>
        <content type="html"><![CDATA[<p><img src="https://chao-x.github.io/post-images/1601295363086.png" alt="" loading="lazy"><br>
<strong>1.基本数据类型所占字节数？所表示的位数？</strong></p>
<p><strong>2.进程和多进程</strong></p>
<p><strong>3.线程和多线程</strong></p>
<p><strong>4.用户级线程，协程</strong></p>
<p><strong>5.进程间通信和线程间通信</strong></p>
<p><strong>6.分区内存管理</strong></p>
<p><strong>7.页式内存管理</strong></p>
<p><strong>8.段式内存管理</strong></p>
<p><strong>9.段页式内存管理</strong></p>
<p><strong>10.虚拟内存</strong></p>
<p><strong>11.生产者消费者与银行家算法</strong></p>
<p><strong>12.DMA</strong></p>
<h2 id="1基本数据类型所占字节数">1.基本数据类型所占字节数</h2>
<p><strong>在C语言中：</strong></p>
<table>
<thead>
<tr>
<th>基本数据类型</th>
<th>32位操作系统</th>
<th>64位操作系统内</th>
</tr>
</thead>
<tbody>
<tr>
<td>char</td>
<td>8位1字节</td>
<td>8</td>
</tr>
<tr>
<td>int</td>
<td>32</td>
<td>32</td>
</tr>
<tr>
<td>short int</td>
<td>16</td>
<td>16</td>
</tr>
<tr>
<td>long int</td>
<td>32</td>
<td>64</td>
</tr>
<tr>
<td>long long int</td>
<td>64</td>
<td>64</td>
</tr>
<tr>
<td>char *</td>
<td>32</td>
<td>64</td>
</tr>
<tr>
<td>float</td>
<td>32</td>
<td>32</td>
</tr>
<tr>
<td>double</td>
<td>64</td>
<td>64</td>
</tr>
</tbody>
</table>
<ul>
<li>C中并没有严格规定short，int，long的长度</li>
</ul>
<p><strong>在Go语言中，</strong></p>
<table>
<thead>
<tr>
<th>数据类型</th>
<th>位数以及表示范围</th>
</tr>
</thead>
<tbody>
<tr>
<td>uint8</td>
<td>无符号 8位整型 (0 到 255)                                                    2的8次方=256</td>
</tr>
<tr>
<td>uint16</td>
<td>无符号 16位整型 (0 到 65535)                                             2的16次方=65536</td>
</tr>
<tr>
<td>uint32</td>
<td>无符号 32位整型 (0 到 4294967295)                                  2的32次方=4294967296</td>
</tr>
<tr>
<td>uint64</td>
<td>无符号 64位整型 (0 到 18446744073709551615)</td>
</tr>
<tr>
<td>int8</td>
<td>有符号 8位整型 (-128 到 127)</td>
</tr>
<tr>
<td>int16</td>
<td>有符号 16位整型 (-32768 到 32767)</td>
</tr>
<tr>
<td>int32</td>
<td>有符号 32位整型 (-2147483648 到 2147483647)</td>
</tr>
<tr>
<td>int64</td>
<td>有符号 64位整型 (-9223372036854775808 到 9223372036854775807)</td>
</tr>
</tbody>
</table>
<p>特殊不定长</p>
<table>
<thead>
<tr>
<th>数据类型</th>
<th>位数以及表示范围</th>
</tr>
</thead>
<tbody>
<tr>
<td>uint</td>
<td>32位操作系统上就是<code>uint32</code>，64位操作系统上就是<code>uint64</code></td>
</tr>
<tr>
<td>int</td>
<td>32位操作系统上就是<code>int32</code>，64位操作系统上就是<code>int64</code></td>
</tr>
<tr>
<td>uintptr</td>
<td>无符号整型，用于存放一个指针</td>
</tr>
</tbody>
</table>
<p><strong>uintptr</strong> 一般用于底层</p>
<p><strong>基本数据类型所表示范围</strong></p>
<p>这次主要注意char,float,double</p>
<p><strong>char</strong></p>
<p>char是有符号char和无符号char主要由编译器决定</p>
<p><strong>float</strong></p>
<p><strong>double</strong></p>
<h2 id="2进程和多进程">2.进程和多进程</h2>
<p>​	进程是资源调度和分配的基本单位。在Linux中，创建一个进程对应一个task_struct结构体，在windows中，对应PCB控制块。进程拥有自己独立的地址空间，一个进程死掉不会对其他进程造成影响。进程具有封闭性和可再现性。</p>
<p>​	在多进程下，子进程崩溃不会影响父进程。在Linux中，通过fork()函数创建一个子进程，在父进程中返回子进程的PID，在子进程中返回0，错误返回负数。子进程和父进程共享代码段。</p>
<blockquote>
<p>​	僵尸进程：在子进程结束exit后，会释放资源，但是会保留pid，需要父进程去回收pid，如果父进程没有调用wait或者waitpid会导致该pid一直被占用，变导致了僵尸进程。而pid是有限的，如果没有pid，即使有再多的资源也无法分配。</p>
</blockquote>
<blockquote>
<p>​	孤儿进程：父进程死掉后，其子进程变为孤儿进程。孤儿进程其实是没有什么危害的，他会被init进程，进程号为0收管，负责回收其pid。所以解决僵尸进程也可以直接结束掉其父进程，让其变为孤儿进程，由init进程来回收它的pid。</p>
</blockquote>
<p>​	在Linux中采用的是多进程模式，虽然Linux也有多线程的概念，实际上操作系统是把他看作进程的。只不过这些进程可以共享一些内存，他们对应的还是task_struct结构体。</p>
<p>​	在windows中，进程是资源的占有者，不是执行者，他的执行需要线程作为载体，一个进程必须有一个线程。</p>
<p>​	不管在linux还是windows，进程都有自己的进程上下文，即进程的状态，寄存器中的值以及堆栈。在内核态下，运行着进程上下文。进程切换时，需要从用户态切换到内核态，对进程的上下文进行切换。此外要需要刷洗TLB。</p>
<blockquote>
<p>用户态：只能访问受限内存。</p>
<p>内核态：在该状态下可以CPU可以访问内存所有数据，包括外围设备：硬盘网卡之类</p>
<p>特权级：对于一个操作系统来说，创建一个进程是核心功能，其会消耗很多资源，这些工作必须有特定的进程去做，于是就有特权级概念。最关键的工作必须交给特权级最高的进程去做，减少资源管理的冲突。x86架构中，一共有四个级别，分别是0-3级，其中0级权限最高，3级权限最低。内核态为0级，用户态为3级。</p>
<p>在内核态下，还运行着中断上下文，用于处理中断。</p>
<p><strong>用户态和内核态之间的转换：</strong></p>
<p>三种方式</p>
<p>1.系统调用</p>
<p>2.异常</p>
<p>3.外围设备中断</p>
</blockquote>
<p>​	这也是进程切换的高代价之一。</p>
<h2 id="3线程和多线程">3.线程和多线程</h2>
<p>​	线程是CPU调度的基本单位。线程是进程的执行体，线程不拥有独立的地址空间，他必须依附于了进程，一个线程死掉，整个进程就死掉。但是线程拥有自己的栈空间和一些必要的寄存器。线程切换的代价比进程切换的要大。</p>
<blockquote>
<p>在多线程中，我们通过说的多线程采用的是内核级线程模型，内核级线程模型的调度是完全由内核决定的，与之相比较的是用户级线程（协程），协程的调度由用户程序自己决定。在内核级线程中，内核会维护一个线程表，内核的切换需要由用户态进入内核态。并且一个线程表的大小也是有限的。</p>
</blockquote>
<blockquote>
<p>这里对切换的理解指定并不是内核里的切换。而是当线程数量大于内核级线程数量时，引起的线程切换。</p>
</blockquote>
<p>​	<strong>什么是线程安全</strong>上面说到栈是线程安全的，是因为栈是线程独有的，其他线程不能修改。我们通常说的线程安全值得是在堆中的全局变量的安全，因为堆中的内存是线程共享的，当在多线程环境下，如果一个线程访问了堆中的变量，并想要做修改，此时另外一个线程也访问了堆中的变量，也想要做修改。他们获取的都是初始的值，都做了不同的修改，数据被污染，最后得到是不正确的结果，这是线程不安全。线程安全就是多个线程之间的切换不会导致执行结果存在二义性。</p>
<h2 id="4用户级线程协程">4.用户级线程，协程</h2>
<p><strong>协程</strong></p>
<p>​	是一种用户态轻量级线程(用户级线程)，协程的调度完全由用户程序控制。协程拥有自己的寄存器上下文和栈。我们都知道一个进程分为用户空间和内核空间，协程的切换不是由内核来管理的，所以不会引起用户态到内核态的转变。</p>
<blockquote>
<p>寄存器上下文:</p>
<p>通用寄存器-8个寄存器，主要用于数据的传输和暂存，可以参与计算以及保存结果</p>
<p>程序寄存器IP-下一条指令的地址</p>
<p>状态寄存器EFLAGS-进位，溢出，正负，奇偶，0</p>
<p>栈指针ESP-栈顶地址</p>
</blockquote>
<p>​	协程的概念是比进程更先提出的。但是线程却更广为人知，原因是协程是非抢占式的，协程的切换由用户程序决定，当一个协程占用CPU时，他就一直占用，除非它需主动让出CPU占有权。这就有一个问题就是当协程发生阻塞或者异常，就一直占用CPU.</p>
<p>​	在协程概念下，多个协程只能使用一个核，并不能达到真正的并行。为了解决真正类似1:1模型的并行，又不让内核来管理线程，使得线程的切换代价太高，又有了M:N混合级线程模型。M个用户级线程自己调度对应N个内核级线程。</p>
<h2 id="5进程间通信和线程间通信">5.进程间通信和线程间通信</h2>
<p>​	<strong>进程间通信</strong>的主要方式有五种：信号量（P/V操作），信号（注销），共享内存，管道，消息队列。</p>
<p>​	信号量可以用于同步和互斥，如生产者消费者问题和银行家算法。</p>
<p>​	信号像是一种通知操作，无需知道目的进程的状态。比如注销产生的sighup信号，CTRL+C产生的程序中止信号</p>
<p>​	共享内存需要对内存区域做同步机制处理</p>
<p>​	管道分为匿名和有名，半双工方式，即同一时刻只能向一个方向流动。匿名管道只能用于有亲缘关系的进程，比如父子，兄弟进程，他是放在内存的文件。而有名管道是放在磁盘的，可以允许任意两个进程。</p>
<p>​	消息队列区别于管道：消息队列是全双工的。虽然都是FIFO，但是也可以随机读取。且可以不用指定目的进程，直接写入消息队列。并且消息队列的大小是大于管道的</p>
<p>​	<strong>线程间通信</strong>可以使用锁，全局变量+共享内存，消息队列，管道...</p>
<h2 id="6分区内存管理">6.分区内存管理</h2>
<p>分区内存管理又分为固定式和可变式。都是为程序分配连续的内存空间</p>
<p>​	固定式内存管理：并不是说每个分配的内存都是一样大的，而是操作系统在程序装入内存前，将所占据的内存可以划分为大小不一，可处理不同规模的内存块。但是分配完后大小便不能再进行改变。</p>
<p>​	这样做有很明显的缺点就是很容易产生零碎的小内存块并且难以利用，并且程序收到内存块大小的制约。</p>
<p>​	可变式内存管理：操作系统在程序装入内存后，在进行大小相同的内存块划分。</p>
<p>​	可变式相比固定式虽然内存利用率相对高一些，但是在用户程序释放内存后，也很容易产生难以利用的小内存块。</p>
<p>​	内存利用率和内存的分配算法有关：1.最佳适应2.最差适应（产生较大的内存块）3.最先适应4.下次适应（从上次分配的地方开始找）</p>
<p>​	为了解决零碎问题以及内存块大小不足（因为是连续内存）可以使用移动技术，但是会增大系统开销</p>
<h2 id="7页式内存管理">7.页式内存管理</h2>
<p>​	相比较分区内存管理，页式内存管理可以为程序分配不连续的内存空间。把逻辑上连续的内存空间映射到不连续的物理内存空间。可以减小分区的零碎内存问题。</p>
<p>​	页式管理的思想是把用户程序和物理内存都分为大小的页。每个用户程序的页映射物理内存的页。通过页表来管理映射管理，使得连续的用户程序页映射到不连续的物理内存页。通过设置页的大小，可以提高内存的利用率。</p>
<p>具体是如何映射的呢？</p>
<p>​	操作系统将逻辑地址分为页号和页内偏移地址，通过页表始址寄存器存储页表的开始位置，页表长度寄存器存储页表的长度。通过和寄存器的长度以及和0对比，先判断逻辑地址的页号是否越界。如果不越界，加上页表始址寄存器的值就得到对应的页号，在页表中，通过页号得到物理地址的块号即物理地址的值。</p>
<p>​	在二级页表的结构中，新增页目录。将逻辑地址分为三部分，页目录，页表和偏移地址。寄存器的始址加上页目录的偏移地址，得到对应的页表的基址，再用此基址加上页目录的偏移地址得到对应的块号。此方法用于虚拟内存中，32位下一般采用10位页目录地址，10位页表地址，12位偏移地址。即1024*1024*4KB=4GB。</p>
<p>​	使用逻辑地址的好处可以避免用户程序直接修改物理内存。相同的逻辑地址可以映射到不同的物理地址，因为始址寄存器的值不一样的。</p>
<p>​	使用页式内存管理也是有缺点的，有时候会将某一个需要保护/共享的内存和不需要保护/不能共享的内存放在同一个页中，在读取该页的地址，容易非法访问某些内存。</p>
<p>​	此外为了减小对页表的读取，采用了快表（TLB转换检测缓存区）的基址，在读页表前先读快表，如果有对应的页号则直接加载，减少了读取页表的压力。在进程切换中，快表也是要进行切换，这也是进程切换的一个很大的代价。</p>
<h2 id="8段式内存管理">8.段式内存管理</h2>
<p>​	段式管理将程序分为许多个程序段，实现了内存的隔离。比如代码分段，数据分段，栈段等等。他将逻辑地址分为段号和段内的偏移地址。通过段表基址寄存器加上段号得到段在物理内存的起址，加上段内的偏移地址即得到真正的物理地址。</p>
<p>段式管理和分区管理的区别在于段式中的不同分段可以是不连续的。</p>
<p>段式内存管理可以解决内存中的共享和保护问题，很容易区分那一块内存是要共享或者保护的，但是段式管理的问题是一个段必须完整的加载到内存中，如果内存大小不够，则程序段就不能被运行，并且段式也容易产生零碎内存区域。</p>
<h2 id="9段页式内存管理">9.段页式内存管理</h2>
<p>​	页式可以很好的减少零碎内存而段式可以很好的对程序的内存进行划分，段页式中和了上面的两种优点。他将逻辑地址分为三部分，段号，页号和页内偏移地址。段表的始址寄存器加上段号得到对应的页表始址，页表始址加上页号得到对应的页块。</p>
<figure data-type="image" tabindex="1"><img src="https://img-blog.csdn.net/20130519103848752" alt="img" loading="lazy"></figure>
<h2 id="10虚拟内存">10.虚拟内存</h2>
<p>​	当一个程序需要的内存大于最大内存时，需要有相关的技术来堆内存扩容。</p>
<p><strong>覆盖：</strong></p>
<p>​	覆盖技术将程序分为若干段，他们不会同时执行，未执行的段存在外存中，当前面一个段在内存中执行完后用后面的段进行覆盖。</p>
<p>​	覆盖技术虽然能够使内存扩容，但是加大了技术难度，要求用户对程序划分出覆盖的结构，增大用户的负担。</p>
<p><strong>交换：</strong></p>
<p>​	交换技术允许一个作业放入内存又放入外存，即在内存和外存间交换，此技术需要保存作业的状态，会增大CPU的运算时间。</p>
<p>​	这两种技术虽然都对内存扩容，但是他们都必须放在连续的内存中</p>
<p><strong>虚拟内存：</strong></p>
<p>​	采用二级页表的方式。</p>
<p>​	在开始前装入一个或零个页面，根据程序需要动态的装入页面，向os申请物理内存，在进程控制信息写入VMA（虚拟内存区域），配置页表信息，在配置页表和物理地址的映射后并不真正的加载到内存中。在我们读取一个页，发现没有真正的放到内存中时，会发生（Page Fault）缺页错误，会去进程控制信息查询是否申请了该内存，如果申请，操作系统会将磁盘中的虚拟内存放入真正的内存（这是正常的）。如果没有会发生内存访问异常（这是异常的），终止程序。</p>
<p>​	CPU将虚拟地址通过MMU（分页内存管理单元）找到相应物理地址。并通过TLB(翻译后备缓冲器，快表)存储映射信息。</p>
<p>​	在页表映射的物理地址基址中，由于是4k的整数倍，后12位一定为0，可以用于判断是否可读可写可执行。</p>
<p>​	在虚拟地址的4G内存空间中，高地址的1GB为内核空间，低地址的3G为用户空间。</p>
<h2 id="11生产者消费者与银行家算法">11.生产者消费者与银行家算法</h2>
<p>生产者消费者</p>
<pre><code class="language-go">func main(){
	ch:=make(chan int,5)
	go func(ch &lt;-chan int){
		for j := range ch {
			fmt.Println(&quot;consumer&quot;+strconv.Itoa(j))
		}
	}(ch)
	go func(ch chan&lt;- int){
		for i:=0;i&lt;3;i++{
			ch&lt;-i
			fmt.Println(&quot;producer:&quot;+strconv.Itoa(i))
		}
	}(ch)


	time.Sleep(time.Second)
}
</code></pre>
<p>银行家算法主要是用来解决死锁的，主要有三个矩阵</p>
<p>MAX进程需要的最大资源矩阵如</p>
<p>{1，2，3}</p>
<p>{2，3，4}</p>
<p>{2，0，3}</p>
<p>Already已经分配的资源</p>
<p>{1，1，1}</p>
<p>{1，2，3}</p>
<p>{1，0，0}</p>
<p>Need还需要的资源</p>
<p>{0，1，2}</p>
<p>{1，1，1}</p>
<p>{1，0，3}</p>
<p>还有系统现有的资源比如{3，2，1}</p>
<p>MAX=Alreay+Need</p>
<h2 id="12dma">12.DMA</h2>
<p>​	DMA允许在没有CPU的干涉下，外部设备直接和存储器之间读写数据。为什么要有DMA呢？因为CPU在处理进程需要的I/O设备时会中断，让I/O设备处理请求，此时进程属于阻塞态，当I/O设备完成请求后，CPU又要去处理I/O设备的数据，这样降低了CPU的效率。如果有一个设备能够直接处理把I/O设备的数据直接与内存交换，而不经过CPU，会提高CPU的效率。</p>
<p>​	DMA在CPU中。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[计算机网络]]></title>
        <id>https://chao-x.github.io/post/ji-suan-ji-wang-luo/</id>
        <link href="https://chao-x.github.io/post/ji-suan-ji-wang-luo/">
        </link>
        <updated>2020-09-18T12:42:42.000Z</updated>
        <content type="html"><![CDATA[<p><strong>1.以太网协议</strong></p>
<p><strong>2.ARP协议</strong></p>
<p><strong>3.IP协议</strong></p>
<p><strong>4.子网掩码</strong></p>
<p><strong>5.路由协议</strong></p>
<p><strong>6.UDP协议</strong></p>
<p><strong>7.TCP协议</strong></p>
<p><strong>8.TCP的拥塞控制</strong></p>
<p><strong>9.HTTP协议</strong></p>
<p><strong>10.HTTPS协议</strong></p>
<p><strong>11.DNS协议</strong></p>
<p><strong>12.NAT协议</strong></p>
<p><strong>13.DHCP协议</strong></p>
<h2 id="1以太网协议">1.以太网协议</h2>
<p>​	我们的主机要和外界互联网进行通信需要加入网络，我可以通过通过光纤，双绞线，无线电报等方式进行网络的连接，它们规定了网络的一些电气特征，主要的作用是传输0和1的电信号。这是物理层的作用。</p>
<p>​	单纯的0和1是没有意义的。在数据链路层中以太网协议将规定了解读电信号的方式。它把0和1进行分组，每一组为一帧。</p>
<p>以太网数据帧由三个部分组成：</p>
<p>头部，数据部，尾部</p>
<figure data-type="image" tabindex="1"><img src="https://img-blog.csdn.net/2018060615493515?watermark/2/text/aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2xpdWNoZW54aWE4/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70" alt="这里写图片描述" loading="lazy"></figure>
<p>​	目的地址和原地址都是MAC地址</p>
<p>​	MAC地址：网卡的地址，48位  6字节   12个16进制数表示 FF-FF-FF-FF-FF-FF*</p>
<p>​	<strong>目的地址</strong>有<strong>三类</strong>：为单播地址，广播地址，多播地址</p>
<blockquote>
<p>以太网数据帧的传输顺序和从左往右传输的，但是目的地址的低位先被传输。如 F1-E2的传输顺序为1F2E</p>
<p>单播地址：一对一传输，目的地址即为目的主机的MAC地址（第一个字节的最低位必须为0）</p>
<p>广播地址：一对多传输，目的地址为FF-FF-FF-FF-FF-FF-FF-FF</p>
<p>多播地址:一对多传输，目的地址最先发送的位必须为1（第一个字节的最低位），这样目的地址不会与任何MAC地址相同，多播数据可以被很多网卡接收</p>
</blockquote>
<p>​	<strong>类型</strong>：表示的是数据属于哪个上层协议（例如0x800，代表IP数据包；0x806，代表ARP数据包）。</p>
<p>在使用网卡进行数据包的发送与接收时，网卡已为我们完成了物理层的所有工作，驱动程序要做的是，在发送数据时，将目的地址、源地址、类型/长度、数据和填充这些值写入网卡，网卡自动计算其CRC并添加在数据帧尾部，同时对数据帧进行物理层的封装，最后将数据帧发送出去</p>
<p><strong>以太网基于CSMA/CD，以CSMA/CD作为MAC<a href="http://lib.csdn.net/base/datastructure">算法</a>的一类LAN称为以太网。CSMA/CD冲突避免的方法：先听后发、边听边发、冲突停止、随机延迟后重发。</strong></p>
<h2 id="2arp协议">2.ARP协议</h2>
<p>​	地址解析协议，ARP协议在TCP/IP协议族中是属于网络层的，而在OSI七层模型中属于数据链路层。</p>
<p>​	它的主要作用是将IP地址和MAC地址相对应。</p>
<p>​	ARP协议是局域网内部协议。它解决的是同一局域网内部的主机或路由器的 <strong>IP 地址</strong>和其对应的<strong>硬件地址</strong>的映射问题。它不能识别其他网络中的MAC地址。实际上ARP协议不也需要解析其他网络，IP协议为它解决了该问题。</p>
<figure data-type="image" tabindex="2"><img src="https://chao-x.github.io/post-images/1601303648769.png" alt="" loading="lazy"></figure>
<p>ARP协议一共有9个字段</p>
<p>字段4为MAC地址类型，以太网类型为1</p>
<p>字段5为要映射的协议类型，IPv4协议为0x0800</p>
<p><strong>字段8为操作类型字段，值为1为ARP请求，2为ARP应答，3为RARP请求，4为RARP应答</strong></p>
<p>ARP请求报文：</p>
<figure data-type="image" tabindex="3"><img src="https://chao-x.github.io/post-images/1601303678474.png" alt="" loading="lazy"></figure>
<p>ARP应答报文：<br>
<img src="https://chao-x.github.io/post-images/1601303703039.png" alt="" loading="lazy"></p>
<p>ARP高速缓存：</p>
<p>​	ARP缓存是一个缓冲区，用来储存IP地址和MAC地址，本质就是&lt;IP地址，MAC地址&gt;的对应表。表中一个条目记录了网络上一个主机的IP地址和其对应的MAC地址。</p>
<p>​	当地址解析协议被询问一个已知IP地址节点的MAC地址时，先在ARP缓存中查看，若存在，就直接返回与之对应的MAC地址，若不存在，才发送ARP请求向局域网查询。</p>
<p>ARP缓存表项的生存时间TTL</p>
<p>ARP缓存包含动态和静态项目：</p>
<p>动态项目随时间推移自动添加和删除，每个动态ARP缓存项都有都设置了TTL(生存时间)，TTL为0时此项目就从表中删除，Windows下TTL一般不超过10分钟。</p>
<p>静态 ARP 缓存条目是永久性的，可以使用 TCP/IP 工具手动添加和删除。<strong>静态 ARP 缓存条目用来对常用的本地IPv4地址（例如路由器和服务器的 IPv4 地址）的ARP请求。</strong></p>
<p>ARP高速缓存的优缺点</p>
<p>优点：从ARP高速缓存的使用中可以看到，ARP高速缓存可以减小广播量，进而减小网络通信量，提高计算机之间的通信效率。</p>
<p>缺点：造成安全隐患（参考下面免费ARP的作用）</p>
<p>免费ARP：</p>
<p>免费ARP指主机发送ARP查找自己的IP地址，通常发生在系统引导期间进行接口配置时。</p>
<p>与标准ARP的区别就是免费ARP分组的目的IP地址字段封装的是自己的IP地址，即向所在网络请求自己的MAC地址。</p>
<p>作用</p>
<p>免费ARP的作用有：<br>
　　1) 一个主机可以通过它来确定另一个主机是否设置了相同的 IP地址。</p>
<p>正常情况下发送免费ARP请求不会收到ARP应答，如果收到了一个ARP应答，则说明网络中存在与本机相同的IP地址的主机，发生了地址冲突。</p>
<p>2)更新其他主机高速缓存中旧的硬件地址进行。</p>
<p>如果发送免费ARP的主机正好改变了硬件地址，如更换了接口卡。</p>
<p>其他主机接收到这个ARP请求的时候，发现自己的ARP高速缓存表中存在对应的IP地址，但是MAC地址不匹配，那么就需要利用接收的ARP请求来更新本地的ARP高速缓存表表项。</p>
<p>3)ARP防御，网关利用免费ARP防止ARP攻击</p>
<p>有些网关设备在一定的时间间隔内向网络主动发送免费ARP报文，让网络内的其他主机更新ARP表项中的网关MAC地址信息，以达到防止或缓解ARP攻击的效果。</p>
<p>4)ARP攻击，利用免费ARP进行ARP攻击</p>
<p>ARP协议并不只在发送了ARP请求才接收ARP应答，<strong>计算机只要接收到ARP应答数据包，就会使用应答中的IP和MAC地址对本地的ARP缓存进行更新。</strong></p>
<p>主机可以构造虚假的免费ARP应答，将ARP的源MAC地址设为错误的MAC地址，并把这个虚假的免费ARP应答发送到网络中，那么所有接收到这个免费ARP应答的主机都会更新本地ARP表项中相应IP地址对应的MAC地址。更新成功后，这些主机的数据报文就会被转发到错误的MAC地址，从而实现了<strong>ARP欺骗</strong>的攻击。</p>
<p>代理ARP：</p>
<p>定义</p>
<p>代理ARP就是通过使用一个主机(通常为路由器)，来作为指定的设备使用自己的 MAC 地址来对另一设备的ARP请求作出应答。</p>
<p>为什么需要代理ARP？</p>
<p>先要了解，路由器的重要功能之一就是把局域网的广播包限制在该网内，阻止其扩散，否则会造成网络风暴。</p>
<p>ARP请求是个广播包，它询问的对象如果在同一个局域网内，就会收到应答。但是如果<strong>询问的对象不在同一个局域网该如何处理</strong>？路由器就提供的代理ARP为这个问题提供了解决方案。</p>
<p>工作过程</p>
<p>两台主机A和B处于同一网段但不同的广播段时，主机A发送ARP请求主机B的MAC地址时，因为路由器不转发广播包的原因，ARP请求只能到达路由器。如果路由器启用了代理ARP功能，并知道主机B属于它连接的网络，那么路由器就用自己接口的MAC地址代替主机B的MAC地址来对主机A进行ARP应答。主机A接收ARP应答，但并不知道代理ARP的存在。</p>
<p>代理ARP的优缺点</p>
<p>优点：代理ARP能在不影响路由表的情况下添加一个新的Router，使子网对该主机变得透明化。一般代理ARP应该使用在主机没有配置默认网关或没有任何路由策略的网络上。</p>
<p>缺点：从工作工程可以看到，这其实是一种ARP欺骗。而且，通过两个物理网络之间的路由器的代理ARP功能其实互相隐藏了物理网络，这导致无法对网络拓扑进行网络概括。此外，代理ARP增加了使用它的那段网络的ARP流量，主机需要更大的ARP缓存空间，也不会为不使用ARP进行地址解析的网络工作。</p>
<p><strong>RARP用于已知硬件地址，而IP地址未知的情况。</strong></p>
<h2 id="3ip协议">3.IP协议</h2>
<p>IP协议为上层协议提供无状态、无连接、不可靠的服务。</p>
<p>IP地址：32位，4个十进制数表示。</p>
<figure data-type="image" tabindex="4"><img src="https://chao-x.github.io/post-images/1601303762965.jpg" alt="" loading="lazy"></figure>
<p>版本号：IP协议版本号，如IPv4</p>
<p>头部长度：单位为4字节（32位），4位最大为15，所以表示<strong>长度最多为64字节</strong></p>
<p>服务类型：区分服务才会被用到，被路由器用来进行流量的优先排序</p>
<p>总长度：单位为1字节，头部长度加上数据长度为总长度。占16位因此总长度最大为2的16次方-1=65535</p>
<p>标识：每产生一个数据报，标识长度+1。注意这里的标识不是指序号，因为IP是无连接服务，因此不存在按序接收。当一个数据报的长度超过网络的最大传输单元必须分片时，相同标识的数据报可以被识别，最后能正确被重装为原来的数据报。</p>
<p>标志：占3位，但目前只有2位有意义。<br>
●　标志字段中的最低位记为MF(More Fragment)。MF=1即表示后面“还有分片”的数据报。MF=0表示这已是若干数据报片中的最后一个<br>
●　标志字段中间的一位记为DF(Don’t Fragment)，意思是“不能分片”。只有当DF=0时才允许分片。</p>
<p>片偏移：单位为8字节。是指分片后相对于原分组的位置。也就是说分片的大小必须是8字节的整数倍</p>
<blockquote>
<p>生存时间：TTL，Time to Live。当为0时，表示过期。</p>
</blockquote>
<p>协议：表明携带的数据是使用何种协议，以便使目的主机的IP层知道应将数据部分上交给哪个处理过程。</p>
<p>首部检验和：当收到一份IP数据包后，同样对首部的每个16位进行反码求和。由于接收方在计算过程中包含了发送发在首部中的校验和，如果传输过程中没有差错，计算结果应全为1，不是全1，即检验和错误。但不包括数据部分。这是因为数据报每经过一个路由器，路由器都要重新计算一下首部检验和（一些字段，如生存时间、标志、片偏移等都可能发生变化）。不检验数据部分可减少计算的工作量。</p>
<p>可选字段：较少用到，长度可变。由于头部必须是4字节的整数倍。当不满足时，填充部分全填充0满足</p>
<p>IP地址分类：</p>
<p><img src="https://chao-x.github.io/post-images/1601303797817.png" alt="" loading="lazy"><br>
1&gt;. A类地址（ 0.0.0.0 - 127.255.255.255 ）以”0”头，网络段长度为8位，其中可变部分的长度为7位；主机段长度为24位。7位的可变网络段可识别2^7=128 (0~127)个网络，其中0和127另有用途，故只有126个可用的A类网络地址。另外，<strong>主机位全”0”代表网络本身，全”1”代表网内广播</strong>，因此一个A类网络地址可识别的可分配地址有 2^24-2 个。</p>
<p>2&gt;. B类地址（ 128.0.0.0 - 191.255.255.255 ）以”10”开头，网络段长度为16位，可变部分的长度为14位；主机段长度为16位。14位的可变网络段可以识别的网络数为 2^14 个。另外，主机位全”0”与全”1”功能同A类地址，因此一个B类网络可以分配地址有 2^16-2 个。</p>
<p>3&gt;. C类地址（ 192.0.0.0 - 223.255.255.255 ）以”110”开头，网络段长度为24位，其中可变部分的长度为21位；主机段长度为8位。21位的可变网络段可以识别的网络数为 2^21 个。可分配的主机地址是 2^8-2 个。</p>
<p>4&gt;. D类地址（ 224.0.0.0 - 239.255.255.255 ）为组播地址，使用”1110”开头，不分网络段和主机段，有 2^28 个组播地址。用于标识预先定义的一组主机。主机使用组播通信时，可以将组播数据报一次性发送给所有同组的主机。</p>
<p>5&gt;. E类地址（ 240.0.0.0 - 255.255.255.255 ）是保留地址，用于研究使用。以”1111”开头，不区分网络段和主机段，其中32位全1代表本网络内广播，因此E类地址共有 2^28-1 个。</p>
<p>A类IP地址 地址范围从1.0.0.1到127.255.255.254 。最后一个是广播地址。<br>
B类IP地址地址范围从128.0.0.1-191.255.255.254 。 最后一个是广播地址。<br>
C类IP地址范围从192.0.0.1-223.255.255.254 。最后一个是广播地址。</p>
<p><strong>适用范围不同：</strong><br>
A类适用的类型为大型网络，A类网络地址数量较少，有126个网络，每个网络支持的最大主机数为256的3次方-2=16777214台；<br>
B类适用的类型为中型网络，B类网络地址数量适中，有16384个网络，每个网络支持的最大主机数为256的2次方-2=65534台；<br>
C类适用的类型为小型网络，C类网络地址数量较多，有209万余个网络，适用于小规模的局域网络，每个网络支持的最大主机数为256的1次方-2=254台。</p>
<h2 id="4子网掩码">4.子网掩码</h2>
<p><strong>私有IP地址和特殊IP地址</strong></p>
<p>根据用途和安全性级别的不同，IP地址还可以大致分为两类：公共地址和私有地址。公用地址在Internet中使用，可以在Internet中随意访问。</p>
<p>一个机构网络要连入Internet，必须申请公用IP地址。但是考虑到网络安全和内部实验等特殊情况，在IP地址中专门保留了三个区域作为私有地址，其地址范围如下：</p>
<p>A类：10.0.0.0/8（子网掩码表示） 10.0.0.0-10.255.255.255<br>
B类：172.16.0.0/12 172.16.0.0-172.31.255.255<br>
C类：192.168.0.0/16 192.168.0.0-192.168.255.255</p>
<p><strong>子网掩码：</strong></p>
<figure data-type="image" tabindex="5"><img src="https://chao-x.github.io/post-images/1601303840304.png" alt="" loading="lazy"></figure>
<p>子网掩码与IP地址进行与操作得到的是网络号，子网掩码取反，与IP地址进行与操作得到的是主机号</p>
<h2 id="5路由协议"><strong>5.路由协议</strong></h2>
<p>​	用于选择路由器</p>
<h2 id="6udp协议"><strong>6.UDP协议</strong></h2>
<p>UDP协议，用户数据报协议，是无连接的，不可靠的协议</p>
<p>它更像是IP协议的封装，是暴露在传出层的接口。为什么不适用IP协议而是用UDP协议呢，是因为IP协议没有端口的概念。如果需要多个信道通信则需要UPD协议</p>
<figure data-type="image" tabindex="6"><img src="https://img-blog.csdnimg.cn/20181130180544486.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dlaXhpbl80MjY4MjgwNg==,size_16,color_FFFFFF,t_70" alt="img" loading="lazy"></figure>
<p>UDP只在IP的数据报服务之上增加了一点的功能，这就是<strong>复用和分用</strong>的功能以及差错检测的功能，UDP的主要特定是：</p>
<p>1.UDP是无连接的，即发送数据之前不需要建立连接，因此减少了开销和发送数据之前的时延。</p>
<p>2.UDP使用尽最大努力交付，即不保证可靠交付，因此主机不需要维持复杂的连接状态表</p>
<p>3.UDP是面向报文的。发送方的UDP对应程序交下来的报文，在添加首部后就向下交付IP层。UDP对应用层交下来的报文既不合并也不拆分，而是保留这些报文的边界</p>
<figure data-type="image" tabindex="7"><img src="https://www.linuxidc.com/upload/2018_09/180924145972613.jpg" alt="img" loading="lazy"></figure>
<p>4.UDP没有拥塞控制</p>
<p>5.UDP支持一对一，一对多，多对一和多对多交换通信。</p>
<p>6.UDP的首部开销小，只有8个字节，比TCP的20个字节的首部要短。</p>
<p><strong>源端口是可选的</strong></p>
<figure data-type="image" tabindex="8"><img src="http://c.biancheng.net/uploads/allimg/191111/6-1911111249535K.gif" alt="img" loading="lazy"></figure>
<p>UDP头部长度为8字节，总长度为2的十六次方减一为65535字节</p>
<p>减去UDP头部长度，再减去IP头部长度20字节（最少）所以UDP数据部分最多为65507字节</p>
<p>UDP协议的优点：快速，相比TCP更安全一些</p>
<p>适用UDP的协议：</p>
<p>DNS协议（域名和IP地址的映射），DHCP协议（应用层协议，一个局域网的网络协议，C/S架构，用于当一个客户机接入局域网后，给它自动分配IP地址和子网掩码），SNMP协议（应用层协议，帮助管理设备的网络性能，发现和解决网络问题），实时的语音 视频</p>
<h2 id="11dns协议">11.DNS协议</h2>
<h2 id="12nat协议">12.NAT协议</h2>
<p>NAT是 Network Address Translation 网络地址转换的缩写。<br>
NAT是将私有IP地址通过边界路由转换成外网IP地址，在边界路由的NAT地址转换表记录下这个转换映射记录，当外部数据返回时，路由使用NAT技术查询NAT转换表，再将目标地址替换成内网用户IP地址。</p>
<p><strong>RFC1918规定了三块专有的地址，作为私有的内部组网使用：</strong></p>
<ul>
<li>A类：10.0.0.0—10.255.255.255      10.0.0.0/8</li>
<li>B类：172.16.0.0—172.31.255.255   172.16.0.0/12</li>
<li>C类：192.168.0.0—192.168.255.255 192.168.0.0/16</li>
</ul>
<p>这三块私有地址本身是可路由的，只是公网上的路由器不会转发这三块私有地址的流量</p>
<p><strong>3.三种NAT技术</strong></p>
<p>静态NAT</p>
<p>静态NAT就是一对一映射，内部有多少私有地址需要和外部通信，就要配置多少外网IP地址与其对应，并不节省外网IP，所以一般不用</p>
<p>NAT静态映射表</p>
<p>动态NAT</p>
<p>动态NAT是在路由器上配置一个外网IP地址池，当内部有计算机需要和外部通信时，就从地址池里动态的取出一个外网IP，并将他们的对应关系绑定到NAT表中，通信结束后，这个外网IP才被释放，可供其他内部IP地址转换使用，这个DHCP租约IP有相似之处。</p>
<p>NAT动态映射表</p>
<p>NAPT  /   PAT(port address Translation，端口地址转换，也叫端口地址复用)<br>
这是最常用的NAT技术，也是IPv4能够维持到今天的最重要的原因之一，它提供了一种多对一的方式，对多个内网IP地址，边界路由可以给他们分配一个外网IP，利用这个外网IP的不同端口和外部进行通信。</p>
<h2 id="13dhcp协议">13.DHCP协议</h2>
<p>DHCP（动态主机配置协议）是一个应用层的局域网的网络协议。指的是由服务器控制一段IP地址范围，客户机登录服务器时就可以自动获得服务器分配的IP地址和子网掩码。</p>
<p>DHCP是基于UDP协议的，为什么是基于UDP而不是TCP?</p>
<p>1.DHCP是通过广播的形式，像DHCP服务器申请分配资源的，TCP是面向连接的，不能广播</p>
<p>2.UDP快速，且DHCP只需要一问一答，没有后面的数据传输，无需建立连接</p>
<p>三种分配方式：</p>
<ol>
<li>
<p>自动分配方式（Automatic Allocation），DHCP服务器为主机指定一个永久性的IP地址，一旦DHCP客户端第一次成功从DHCP服务器端租用到IP地址后，就可以永久性的使用该地址。</p>
</li>
<li>
<p>动态分配方式（Dynamic Allocation），DHCP服务器给主机指定一个具有时间限制的IP地址，时间到期或主机明确表示放弃该地址时，该地址可以被其他主机使用。</p>
</li>
<li>
<p>手工分配方式（Manual Allocation），客户端的IP地址是由网络管理员指定的，DHCP服务器只是将指定的IP地址告诉客户端主机。</p>
</li>
</ol>
<p>三种地址分配方式中，只有动态分配可以重复使用客户端不再需要的地址。</p>
<h2 id="14snmp协议">14.SNMP协议</h2>
<p>在大型网络管理中，网络管理员比较头痛的问题就是如何实时了解不在身边的网络设备的运行状况。若要一台一台的去查看网络设备的运行现状，那明显不是很现实。SNMP协议可以自动帮助管理员收集网络运行状况的方法。通过这种方法，网络管理员只需要坐在自己的位置上，就可以了解全公司的网络设备的运行情况。</p>
<p>SNMP，其中文名字叫做简单网络管理协议，这是一个应用层协议。有了这个简单网络管理协议，则网络管理员可以很方便的在SNMP代理和管理器之间交换管理信息。它的主要作用就是帮助企业网络管理人员更方便的了解网络性能、发现并解决网络问题、规划网络的未来发展。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Go语言]]></title>
        <id>https://chao-x.github.io/post/go-yu-yan/</id>
        <link href="https://chao-x.github.io/post/go-yu-yan/">
        </link>
        <updated>2020-09-09T01:00:28.000Z</updated>
        <content type="html"><![CDATA[<p><strong>1.编译型语言，解释型语言，脚本语言的区别？什么是标记语言，编程语言</strong></p>
<p><strong>2.什么是静态型？强类型？弱类型？</strong></p>
<p><strong>3.Go语言的特性，相比较Java等其他语言有什么区别？</strong></p>
<p><strong>4.Go语言的语法，哪些常用函数？</strong></p>
<h3 id="1编译型语言解释型语言脚本语言的区别">1.编译型语言，解释型语言，脚本语言的区别？</h3>
<p>首先理解什么是编译型语言什么是解释型语言。</p>
<p>计算机是不能理解高级语言的，需要将高级语言翻译成二进制</p>
<p>编译型语言：</p>
<p>通过编译器翻译高级语言，它是在执行前将整个程序进行翻译，生成二进制可执行文件，直接运行。</p>
<p>解释型语言：</p>
<p>通过解释器翻译高级语言，在执行前不需要编译，它是在执行时对程序逐行翻译成机器语言，并直接执行，不会生成二进制文件</p>
<p><strong>编译型语言会输出二进制文件，解释型语言只会输出运行结果</strong></p>
<p>标记语言：</p>
<p>被读取的，本身没有行为能力（被动）；例如：Html 、XML等</p>
<p>编程语言：</p>
<p>本身具有逻辑性和行为能力；例如：C、Java等</p>
<p><strong>脚本语言是区于标记语言和编程语言之间的语言，它的特点是无类型，无内存管理，它是一种解释型语言</strong></p>
<p><strong>Java语言</strong></p>
<p>Java是一种先编译后解释的语言，它既有编译语言的特性又有解释语言的特性</p>
<p>Java的翻译过程：</p>
<p>首先有编译器（如javac）编译成字节码文件（.class文件），再由JVM虚拟机对字节码文件进行解释。</p>
<p>生成字节码的作用:</p>
<p>1.字节码更便于虚拟机读取，不用在解析字符串，所以运行速度比直接解析源代码快。</p>
<p>2.语法是会变的，而源代码中没有版本信息，而字节码中不但有版本信息，还可以经由编译过程抹平一些语言层面的变化，翻译成字节码后就用在区分版本</p>
<p>3.字节码也可以由其他语言生成，如**<a href="https://link.zhihu.com/?target=http%3A//www.oschina.net/p/groovy">Groovy</a>，<a href="https://link.zhihu.com/?target=http%3A//www.oschina.net/p/clojure">Clojure</a>，<a href="https://link.zhihu.com/?target=http%3A//www.oschina.net/p/scala">Scala</a>**。需要注意的事，既然这些语言可以编译成字节码，也就可以被Java或其他JVM语言调用。</p>
<p><strong>Go语言</strong></p>
<p>Go原因是</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JWT]]></title>
        <id>https://chao-x.github.io/post/jwt/</id>
        <link href="https://chao-x.github.io/post/jwt/">
        </link>
        <updated>2020-02-18T13:02:08.000Z</updated>
        <content type="html"><![CDATA[<h1 id="jwt">JWT</h1>
<h2 id="token令牌">Token令牌</h2>
<p>不是在每一次请求时提供用户名和密码的凭证。我们可以让用户通过token交换凭证，这个token提供用户访问服务器的权限。Token通常比密码更加长而且复杂。比如说，JWT通常会应对长达150个字符。一旦获得了token，在每次调用API的时候都要附加上它。然后，这仍然比直接发送账户和密码更加安全，哪怕是HTTPS。</p>
<h2 id="jwt工作原理">JWT工作原理</h2>
<p>例如：首先浏览器向服务器发送登录请求，当用户名密码正确时，服务器生成jwt令牌返回浏览器端，以后浏览器访问服务器时，通过该令牌即可完成相应操作。</p>
<h2 id="什么是-jwt">什么是 JWT ？</h2>
<p>JWT -- JSON Web Token，它是基于 RFC 7519 所定义的一种在各个系统中传递紧凑和自包含的 JSON 数据形式。</p>
<ul>
<li>紧凑（Compact） ：由于传送的数据小，JWT 可以通过GET、POST 和 放在 HTTP 的 header 中，同时也是因为小也能传送的更快。</li>
<li>自包含（self-contained） : Payload 中能够包含用户的信息，避免数据库的查询。</li>
</ul>
<p>JSON Web Token 由三部分组成使用 . 分割开：</p>
<ul>
<li>Header</li>
<li>Payload</li>
<li>Signature</li>
</ul>
<p>一个 JWT 形式上类似于下面的样子：</p>
<blockquote>
<p>xxxxx.yyyy.zzzz</p>
</blockquote>
<h4 id="header">Header</h4>
<p>一般由两个部分组成：</p>
<ul>
<li>alg</li>
<li>typ</li>
</ul>
<p>JWT 头描述了 JWT 元数据，是一个 JSON 对象，格式如：<strong>json{&quot;alg&quot;:&quot;HS256&quot;,&quot;typ&quot;:&quot;JWT&quot;}</strong></p>
<p>alg 属性表示签名所使用的算法。JWT 签名默认的算法为 HMAC SHA256 ， alg 属性值 HS256 就是 HMAC SHA256 算法。</p>
<p>typ 属性表示令牌类型，这里就是 JWTPayload</p>
<p>Header会被 Base64编码成第一部分。</p>
<h4 id="payload">Payload</h4>
<p>这一部分是 JWT 主要的信息存储部分，其中包含了许多种的声明（claims）。</p>
<p>Claims 的实体一般包含用户和一些元数据，这些 claims 分成三种类型：reserved, public, 和 private claims。</p>
<ul>
<li>（保留声明）reserved claims ：预定义的 一些声明，并不是强制的但是推荐。它们包括:
<ul>
<li>iss (issuer) 该JWT的签发者</li>
<li>exp (expiration time) 什么时候过期，这里是一个Unix时间戳</li>
<li>sub (subject) 该JWT所面向的用户</li>
<li>aud(audience) 接收该JWT的一方</li>
<li>iat(issued at): 在什么时候签发的</li>
<li>这里都使用三个字母的原因是保证 JWT 的紧凑</li>
</ul>
</li>
<li>（公有声明）public claims : 这个部分可以随便定义，但是要注意和 IANA JSON Web Token 冲突。</li>
<li>（私有声明）private claims : 这个部分是共享被认定信息中自定义部分。</li>
</ul>
<p>一个 Pyload 可以是这样子的：</p>
<pre><code class="language-json">{
  &quot;iss&quot;: &quot;joe&quot;,
  &quot;exp&quot;: 1300819380,
  &quot;sub&quot;: &quot;1234567890&quot;,
  &quot;name&quot;: &quot;John Doe&quot;,
  &quot;admin&quot;: true
  &quot;iat&quot;: 1441593502,
  &quot;aud&quot;: &quot;www.example.com&quot;,
  &quot;from_user&quot;: &quot;B&quot;,
  &quot;target_user&quot;: &quot;A&quot;
}
</code></pre>
<p>这部分同样使用 Base64编码成第二部分。这里注意Base64是可逆的，不要存放敏感信息，如密码等。</p>
<h4 id="signature">Signature</h4>
<p>在创建该部分时候你应该已经有了 编码后的 Header 和 Payload 还需要一个秘钥，这个加密的算法应该 Header 中指定。</p>
<p>一个使用 HMAC SHA256 的例子如下:</p>
<p>即用第一部分和第二部分通过base64Url编码后，加上服务器秘钥（非常重要）组成的一个字符串，通过HMAC SHA256编码后得到。</p>
<pre><code class="language-javascript">HMACSHA256(
  base64UrlEncode(header) + &quot;.&quot; +
  base64UrlEncode(payload),
  secret)
</code></pre>
<p>这个 signature 是用来验证发送者的 JWT 的同时也能确保在期间不被篡改。</p>
<p>所以，最后你的一个完整的 JWT 应该是如下形式：</p>
<pre><code class="language-css">eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiYWRtaW
</code></pre>
<h2 id="代码">代码</h2>
<p>核心 Util 类代码如下：</p>
<pre><code class="language-java">public class JwtUtil {
	private String final key=&quot;thisisakey&quot;;
    
    public static String getToken(Map&lt;String,String &gt;map)
    {
        //获取过期时间
        Calendar instance=Calendar.getInstance();
        instance.add(Calendar.Date,7);//设置为7天
        
        //创建jwt实例
        JWTCreator.Builder builder=JWT.creat();
        
        //设置payload内容
        map.forEach((k,v)-&gt;{
           builder.withClaim(k,v); 
        });
        
        //指定令牌过期时间并通过秘钥进行HS256编码
        String token =builder.withExpiresAt(instance.getTime())
				.sigh(Algorithm.HMAC256(KEY));
        return token;
    }
    
    //验证令牌
    public static DecodedJWT verify(String token)
    {
        return JWT.require(Algorithm.HMAC256(KEY)).build().verify(token);
    }

}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Git]]></title>
        <id>https://chao-x.github.io/post/git/</id>
        <link href="https://chao-x.github.io/post/git/">
        </link>
        <updated>2020-01-28T13:04:22.000Z</updated>
        <content type="html"><![CDATA[<h1 id="什么是git">什么是Git?</h1>
<p>​	与SBV管理相比：（SBV是集中式管理，Git是分布式管理）</p>
<p>SBV:将它们存储的信息看作是一组基本文件和每个文件随时间逐步累积的差异</p>
<figure data-type="image" tabindex="1"><img src="https://git-scm.com/book/en/v2/images/deltas.png" alt="存储每个文件与初始版本的差异。" loading="lazy"></figure>
<p>Git:更像是把数据看作是对小型文件系统的一系列快照。 在 Git 中，每当你提交更新或保存项目状态时，它基本上就会对当时的全部文件创建一个快照并保存这个快照的索引。 为了效率，如果文件没有修改，Git 不再重新存储该文件，而是只保留一个链接指向之前存储的文件。 Git 对待数据更像是一个 <strong>快照流</strong>。</p>
<figure data-type="image" tabindex="2"><img src="https://git-scm.com/book/en/v2/images/snapshots.png" alt="Git 存储项目随时间改变的快照。" loading="lazy"></figure>
<h1 id="git-配置">Git 配置</h1>
<p>安装完 Git 之后，要做的第一件事就是设置你的用户名和邮件地址。 这一点很重要，因为每一个 Git 提交都会使用这些信息，它们会写入到你的每一次提交中, 不可更改：</p>
<pre><code class="language-console">$ git config --global user.name &quot;John Doe&quot;
$ git config --global user.email johndoe@example.com
</code></pre>
<h4 id="检查配置信息">检查配置信息</h4>
<pre><code class="language-bash">git config --list
</code></pre>
<p><code>git config &lt;key&gt;</code>： 来检查 Git 的某一项配置</p>
<pre><code class="language-bash">$ git config user.name
John Doe
</code></pre>
<h1 id="帮助">帮助</h1>
<p>若你使用 Git 时需要获取帮助，有三种等价的方法可以找到 Git 命令的综合手册（manpage）：</p>
<pre><code class="language-bash">$ git help &lt;verb&gt;
$ git &lt;verb&gt; --help
$ man git-&lt;verb&gt;
</code></pre>
<p>例如，要想获得 <code>git config</code> 命令的手册，执行</p>
<pre><code class="language-bash">$ git help config
</code></pre>
]]></content>
    </entry>
</feed>